# Claude Code — local machine overrides (paths, personal env vars)
.claude/settings.local.json
.claude/settings.json.bak

# External skills cloned by the init script (nested git repos — not redistributed)
.claude/skills/playwright-best-practices/

node_modules/
*.log
.DS_Store
.env
.env.*
!.env.example
*.pyc
__pycache__/
.venv/

# Mobile QA — binary artifacts, ephemeral test screenshots, local guides
*.apk
*.ipa
reports/screenshots/
bundles/mobile-qa/QUICKSTART-LOCAL.md

# Agent working directories — generated at onboarding/run time, not distributed
.agents/

# Test cases and suite plans — project-specific, not distributed with the repo
tasks/