# Claude Code — installed config and hooks (generated by init script, not distributed)
.claude/

# MCP config — contains local paths; copy from .mcp.json.example
.mcp.json

node_modules/
*.log
.DS_Store
.env
.env.*
!.env.example
*.pyc
__pycache__/
.venv/

# Mobile QA — binary artifacts, ephemeral test screenshots, local guides
*.apk
*.ipa
reports/screenshots/
bundles/mobile-qa/QUICKSTART-LOCAL.md

# Agent working directories — generated at onboarding/run time, not distributed
.agents/

# Test run reports — generated per project, not distributed
reports/

# Test cases and suite plans — project-specific, not distributed with the repo
tasks/
# Local-only brainstorming/spec/plan scaffolding (not distributed)
docs/superpowers/
