# Changelog — security-testing

## Unreleased

- Bundle skeleton: `factory.json` roster (three agents, five skills),
  `FACTORY.md` catalog descriptor, `instructions.md`, and the seeded
  `knowledge/` docs (`engagement.md.template`, `finding-schema.md`,
  `report-template.md`). Agents and skills are placeholders until the
  scripts and prose land; the `1.0.0` entry is written with them.
