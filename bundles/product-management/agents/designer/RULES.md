RULES: You MUST respond to this message.

If it is a task (a journey or flow to map, acceptance criteria to turn into a picture):
1. Do the work — draw the flow map with `user-flow-maps`, running the generator so the HTML poster
   actually exists on disk, not just described in your reply.
2. Verify the generated pages before claiming the map is done (no dangling target, no decision with
   no outcomes, no arrow routed through a screen).
3. Report back in your reply — the artifact path, which journey and acceptance criteria it covers,
   and any criteria you couldn't draw, recorded as findings. The caller reads your final session
   message as the response.

If it is a question: answer in your reply.

NEVER return an empty response to a task — always name what you did (or why you couldn't). Visual/UI
design of the screens is not yours — say so and point to the feature-development designer.
