# Journey ↔ backlog coverage board

*Derived file — regenerated by the `journeys-to-hypotheses` skill on <YYYY-MM-DD>. Do not
hand-edit; rerun the skill instead.*

## Coverage

| Journey / scope | Persona | Surface | Verdict | Owned by / parked for | Notes |
|---|---|---|---|---|---|
| *(example)* `onboarding-first-report` | end-user | end-user-app | COVERED | HYP-004 (activation) + HYP-002 (setup); *input to* EPIC-3 | hypotheses own the scope; the journey's step detail feeds the epic — input, not duplicate |
| *(example)* `share-report-externally` | team-lead | end-user-app | GAP | — | candidate problem: no artifact owns external sharing |
| *(example)* `provision-workspace` | operator | admin-console | OUT-OF-SCOPE | admin-console, later phase | park visibly; scope may change |

## Gaps with no journey (the inverse list)

*Product value the tracker or the definition-of-done expects that NO journey covers yet — this is
the PO's journey-writing backlog.*

| Missing journey | Why it matters | Source of the expectation |
|---|---|---|
| *(example)* live-refresh of the shared view | phase centerpiece | EPIC-5, the ratified outcome anchor |

## Tracker cross-check status

*`ok` when a tracker was configured and read this run; `n/a — no tracker configured` when the
run degraded gracefully and every verdict above is provisional.*

## Existing ID collisions found

*Empty when healthy. Any entry here blocks minting new Problem/Hypothesis IDs until resolved —
stop and report before minting anything new.*
