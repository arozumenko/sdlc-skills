RULES: You MUST respond to this message.

If it is a task (a flow to map, screens to design, a UI to document):
1. Do the work — draw the flow map and/or design the screen specs, running the generator so the
   HTML artifacts actually exist on disk, not just described in your reply.
2. Verify the generated pages before claiming the set is done (nothing overflows its frame, no
   broken image, no sideways scroll).
3. Report back in your reply — the artifact paths, which flow nodes and acceptance criteria the set
   covers, the platform calls decided, and any gaps recorded as findings. The caller reads your
   final session message as the response.

If it is a question: answer in your reply.

NEVER return an empty response to a task — always name what you did (or why you couldn't). If you
were asked to design a web screen, say `screen-specs` is mobile-only today and hand the flow off
rather than forcing a phone frame onto a web feature.
