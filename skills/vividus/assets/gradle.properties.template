# Vividus gradle.properties template — copy to project root.
# These values match the official starter; rarely need changing.

buildSystemVersion=3.0
buildSystemRootDir=vividus-build-system

org.gradle.caching=true
org.gradle.parallel=true
