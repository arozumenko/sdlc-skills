# delivery-metrics M1 plan v1 — adversarial review (codex gpt-6-astra, round 1)

Date: 2026-09-17

## Verdict

needs-changes

19 blockers / 14 majors / 0 minors

Executing the pasted plan will fail multiple task gates. More seriously, its identity/resolution, version replay, parent boundaries, hook admission, coverage and sync paths can produce incorrect or unsupported delivery figures. Repairs should preserve the specified append-only, advisory-lock, best-effort design; no stronger storage machinery is requested.

## Findings

Evidence references to “Plan” are line numbers in `docs/superpowers/plans/2026-09-17-delivery-metrics-m1-slice1.md` (v1); “Spec” refers to `docs/superpowers/specs/2026-09-16-delivery-metrics-design.md` (v5.1). Executable checks used Node v24.14.1 with extracted in-memory modules; no implementation files were created and no full repository test pass is claimed.

| id | severity | slug | task/step | finding | evidence | concrete change |
|---|---|---|---|---|---|---|
| F1 | blocker | markdown-group-parser | Task 4 steps 1, 3–4 | The importer cannot pass its fixture: its fallback runs only when the primary regex finds no number. G0 already yields 001, so 055 is moved to ungrouped. It also scans outside the execution block and has no continuation-line handling. | Plan 915–918, 962–963, 1018–1034, 1059. Executing the supplied module in memory on Node 24.14.1 produced G0=[TASK-001], ungrouped=[TASK-055,TASK-099]. | Parse the bounded execution block, including columns and continuation lines, while excluding dependency references in annotations. Keep the existing expected G0 membership and add continuation/dependency counterexamples. |
| F2 | blocker | registration-retry-rejected | Task 5 steps 1, 3 | An identical registration retry exits 2 before reaching append/dedup. After an interruption between savePlan and appends, the same check also prevents the promised retry from emitting missing registration observations. | Plan 1142–1145 expects exit 0 and SKIP; 1305–1307 rejects every version <= previous; 1328–1329 saves before appending. Spec §6.3 explicitly requires emission on every retry, not only a catalogue diff. | Recognize an identical request/version before rejecting stale versions; retain enough ordinary registration request data to regenerate its observations with the same identities and clocks. Emit the completed ID-bearing block. Add a save-before-append interruption fixture; do not introduce transactions. |
| F3 | blocker | metrics-fixture-exclusion-count | Task 7 steps 2, 4–5 | The hand-computed test expects three unestimated exclusions, but the implementation skips clock-skewed t5 before examining its estimate. Only t4 and t7 increment that counter, giving two. | Plan 1825–1832, 1854, 1965–1968. The supplied four tests executed through in-memory modules on Node 24.14.1: 3 pass, 1 fails, actual 2 vs expected 3. | Define exclusion counting explicitly, including overlapping data-completeness counts versus mutually exclusive eligibility reasons, and make the implementation and hand calculation agree. Preserve a visible count for the unestimated skewed item if using the existing expected value. |
| F4 | blocker | report-fixture-failures | Task 8 steps 1, 3, 5 | Two report assertions cannot pass: default since is returned verbatim without milliseconds, and the open-item age is hand-calculated incorrectly. | Plan 2054 supplies 2026-09-07T00:00:00Z; 2067 expects .000Z; 2170 returns the original string. Plan 2060 starts TASK-B at Sep 10 09:00 and 2062 ends Sep 21 00:00: elapsed is 255 h, but the status test at 2097 expects 254. | Normalize all window endpoints before comparison/output, including default observation_start. Correct the age assertion to 255 h and retain the fixed-clock fixture. |
| F5 | blocker | backfill-test-existing-branch | Task 9 steps 1, 5 | The positive backfill test ends by creating task/task-002 even though its helper already created and retained that branch. Git exits 128. The intended negative second-parent-evidence test is never constructed. | Plan 2363–2370 creates task/task-002; 2400 runs checkout -b task/task-002 again; no branch deletion occurs. | Create a separate temporary repository or a genuinely unused declared alias for the negative case, commit unrelated work, merge it, invoke the adapter, and assert the missing-evidence note and absence of done. |
| F6 | blocker | golden-extractor-invalid-dataset | Task 10 steps 1–4 | The supplied extractor does not produce the required dataset and its memberships cannot register. It treats dependency 004 as G2 membership and drops the G6 continuation tasks. Its rework regex also misses two of the claimed eleven proxy items. | Plan 2569–2577, 2582, 2602. Running the exact extractor in memory against locally available b1f70d2 returned 59 tasks / 30 groups / 33 merged / 9 rework items, duplicate TASK-004, and missing TASK-032, TASK-058, TASK-059. validatePlan rejects the duplicate. | Ship a validated, pinned transformed dataset with source path/revision/hash. Parse real group declarations and all evidenced rework forms, reconcile 59/30/33/11 against source evidence, and assert unique complete membership before building the fixture. Replace the author-specific checkout path with a repository-relative extraction procedure. |
| F7 | blocker | revision-conflict-fallback | Tasks 2, 6, 8 — resolution pipeline | Revision conflict handling violates both supersession and no-fallback rules. Any historical conflicting revision permanently excludes an observation, even after a unique higher revision. A currently conflicting CLI observation is then removed before occurrence selection, allowing git to win; reports discard ledger-level conflicts entirely. | Plan 537–542, 1625–1638, 2173–2174, 2188, 2196–2197. In-memory execution of resolveObservations with conflicting rev0 plus unique rev1 returned active=[] and one conflict. Spec §6.2 requires higher unique revisions to supersede and current conflicts to block fallback at their source rank. | Resolve only the highest revision for current conflict/retraction state. Carry conflict occurrence/basis/rank metadata into source selection, quarantine affected occurrences without lower fallback, and include both ledger and occurrence conflicts in reports. Test both arrival orders and corrected conflict recovery. |
| F8 | blocker | generation-semantic-identity | Tasks 2, 3, 6 — identity and equivalence | Different runs can collapse into the same observation, and materially different dispatch facts can be treated as equivalent. Generated item IDs repeat across campaigns/runs; source tokens such as merge SHA or done-NNN repeat too. semanticKey omits plan and stage, while factKey omits episode/causal/interval/review/gate/disposition facts. | Plan 444–445, 491–497, 797–803, 1623, 2463–2464, 3207. In-memory semanticKey evaluation returned equality for otherwise identical records from c/run-1/1 build and c/run-2/1 review. Spec §§5, 6.2 and D6. | Make source-record identity generation-qualified or stable item identity globally scoped, preserving the injective tuple contract. Canonicalize all semantic identity and transition facts separately from capture/provenance envelopes. Add reused ref/token/SHA across independent runs and build-versus-review disagreement tests. |
| F9 | blocker | recut-history-disconnected | Tasks 3, 5, 8 — catalogue/replay | A v2 plan only receives its delta events, but reporting v2 filters out every v1 observation. Unchanged tasks lose creation, dispatch, completion and accepted estimates. Reporting all versions produces separate disconnected populations instead of one stable-item history; older versions remain open, so unqualified commands become ambiguous after a normal version bump. | Plan 1305–1325, 1274–1276, 2177–2182. Catalogue rows at 809–811 lack generation and validity intervals. Spec §§5, 6.3, 6.9; AC-15. | Replay all applicable versions within each campaign/run, union stable IDs once, apply membership/class/parent at the endpoint effective version, and keep independent runs separate. Require effective --at for re-cuts and define the active capture version. Test carried-in dispatch/done/estimate through v2 and historical cutoffs. |
| F10 | blocker | invalid-stable-id-migration | Tasks 3, 5 — validation and re-cut | Explicit duplicate item_id values are accepted, and deterministic slug assignment can collide even when refs are distinct. buildTimelines then overwrites entries in its Map. Migration validation checks only old map keys; nonexistent target IDs, unrelated supersedes.plan_id, missing continuity mappings, and cycles caused by reused parent IDs pass. The map is never applied. | Plan 758–792 validates refs, not IDs; 797–803 assigns lossy slugs; 1308–1310 checks old keys only; 1667 builds Map by item_id. Spec §5 identity/parentage/re-cut. | Validate the completed ID-bearing tree for unique IDs, legal parentage and cycles; reject or disambiguate generated collisions. Validate both mapping endpoints and the actual predecessor, require mappings where continuity is claimed, and apply mappings during history resolution without copying split/merge completions. |
| F11 | blocker | parent-integration-boundary | Task 6 step 3 | All-terminal children automatically complete missions/campaigns without integration evidence. Conversely, an explicit parent done can retain a timestamp earlier than a child terminal clock. Explicit parent dispatch overrides the required earliest descendant start, and scope-cancelled children are removed from history and partial-cancellation counts. These yield wrong parent elapsed and schedule variance. | Plan 1574–1587 enshrines evidence-free completion; 1702–1714 implements it. Spec §5 requires integration boundary and max(child terminal, integration), derived descendant start, and partial/all cancellation counts; §6.4 fixes parent actual selection. | Require the declared integration evidence before parent delivery, derive both boundaries from eligible descendants plus integration, and retain historical cancelled children and their terminal clocks. Add pending landing, early explicit done, partial/all cancellation and removed-delivered-child fixtures. |
| F12 | blocker | invalid-transition-replay | Tasks 5, 6 — event validation/replay | The CLI never validates a transition against history. Replay accepts cancelled→done without a reopen, resolves incompatible same-time terminal events by string ordering, and treats any dispatched basis as an observed start. A later dispatch can set start after done. Reopened is actively derived despite being deferred in M1. | Plan 1338–1353, 1645, 1676–1692; INVALID-TRANSITION is declared but never raised. Spec §§6.2, 6.9, §13 M1. | Validate M1 transitions before CLI append and quarantine/count invalid imported chains, with explicit causal handling for ties. Only observed build dispatch may start task cycle time. Count reopen/review/block episode events as deferred in M1; do not partially implement their state semantics. |
| F13 | blocker | rounding-before-accuracy | Task 7 step 4 | Durations are rounded to two decimal hours before ratio, hit-rate and MRE calculations. A real 15-second cycle becomes actual=0, changing zero-actual exclusions and range hits. | Plan 1890–1891, 1943–1949, 1971–1979. Executing hoursBetween for 00:00:00→00:00:15 returned 0. Spec D10/D12, §§6.4, 6.10: seconds internally, compute before rounding, retain genuine sub-minute durations. | Keep precise seconds or unrounded hours through all statistics and eligibility decisions; round only rendered/export display values. Test a sub-minute actual against an exact narrow range and near-boundary MRE/PRED values. |
| F14 | blocker | duration-validity-not-per-metric | Task 7 step 4 | The implementation excludes retrospective lead solely from short duration and plan-commit basis, without same-commit evidence. It also drops all durations when cycle is skewed, even a valid lead, and can include negative lead/commit_to_done when only those clocks are skewed. | Plan 1720 flags only start/done skew; 1942 skips all metrics; 1945 and 1948 admit negative values; 1949 drops short plan proxies. Test 1831–1841 encodes the unsupported retrospective assumption. Spec D12, §§6.9–6.10; AC-13. | Track source-commit evidence for creation/completion and apply the retrospective rule only when the same commit is demonstrated. Validate each duration independently; preserve valid metrics and throughput when another clock is skewed. Correct the retrospective fixture and add mixed-validity cases. |
| F15 | blocker | velocity-before-declared-coverage | Tasks 7, 8 — window/throughput | Weekly series use the shared requested window without consulting each plan’s observation_start or full declared coverage. A later-starting plan receives fabricated zero whole weeks and can gain a velocity before three covered weeks exist. | Plan 1934, 1954–1958, 2170, 2181. In-memory computeMetrics with coverage starting Sep 8 but since Aug 31 returned W36=0 and W37=1 both whole, and a four-week velocity. Spec D11, §6.9, AC-15. | Intersect each plan’s declared coverage with the requested window, mark partially covered weeks, and derive velocity only from fully covered weeks. Use a common covered interval or separate per-plan intervals, with boundary/conservation fixtures. |
| F16 | blocker | estimate-acceptance-reuse | Tasks 3, 5 — estimate validation/delta | A changed range or tier can reuse the old acceptance pair and count as timely human-accepted work. The plan’s own v2 test changes high from 3 to 5 while retaining EST’s original acceptance. Calibrated estimates also pass without the required reference evidence. | Plan 739–753, 840–850; 1151–1161 reuses EST. Spec §6.4 requires a newly declared pair after range/unit/tier change and saved reference path/hash/population for calibrated tier. | Validate acceptance changes against the prior estimate; require a newly declared acceptance pair or mark the changed proposal unaccepted. Validate calibrated provenance and preserve original accepted estimates separately. Add unchanged-pair and missing-calibration-reference rejection tests. |
| F17 | blocker | hook-unvalidated-child-clocks | Task 11 step 3 | Any existing agent_transcript_path is trusted without checking host/session/agent correlation. A file containing one timestamp and a user message is sufficient to emit both measured start and end, even when it is partial or belongs to another child. | Plan 2852–2859, 2864–2875, 2923–2937. Spec §6.6 requires validated path/session association and completion evidence; missing/partial transcripts remain unknown/incomplete. | Admit clocks only from supported, correlated child transcript shapes with completion evidence. Reject mismatched session/agent paths and represent incomplete capture diagnostically. Add swapped-child, foreign-session, user-only and truncated transcript fixtures. |
| F18 | blocker | backfill-wrong-completion-history | Task 9 step 3 | The adapter selects the newest qualifying merge as episode-1 because git log is reverse chronological and later matches are skipped. It never verifies --head lies on the declared integration ref. First-commit selection takes the oldest same-ref commit before applying the generation epoch, so a previous run can prevent discovery of the current run’s first commit. | Plan 2474–2478, 2485–2492, 2508–2509; source_epoch.integration_ref is never used for git validation. Spec §§6.7, 6.9: first qualifying completion, generation-bound history, pinned integration head. | Validate the pinned head against the declared integration history; filter by generation before selecting first commit; select the earliest qualifying completion in causal history. Add repeated task merges, reused refs across epochs and non-integration-head tests. |
| F19 | blocker | sync-stages-unresolved-conflicts | Task 5 step 3 — bestEffortSync | Blind git add -A followed by commit can stage unresolved conflict-marker files and commit them as a resolved merge. That violates the explicit promise to surface git conflicts and never automatically resolve them; later readers can treat surviving lines as valid data. | Plan 1416–1426 stages before checking merge/conflict state. Spec D4, §6.1, §8, AC-16 require unresolved state to be surfaced and unresolved sources excluded. | Before staging, inspect unmerged paths and unfinished merge state; leave those states untouched and report sync pending/manual repair. Exclude unresolved source files from report derivation with counts. Keep this a best-effort check, without adding cross-writer locks. |
| F20 | major | ledger-schema-not-validated | Task 2 step 3 | readRaw accepts any v2 object with observation_id and an integer revision, including negative revisions, absent clocks, invented sources/events and invalid estimates. Later sorting can throw on missing at; invalid UTF-8 is silently replacement-decoded. Skipped malformed rows do not expose path/line diagnostics. | Plan 505–519, 545; makeObservation at 446–488 is not called on read and is itself permissive. Spec §§6.1–6.2 require malformed UTF-8/JSON/schema rows to be skipped and counted with locations. | Use a shared full record validator on append/import/read, with strict UTC-ms clocks and nullable-item restrictions. Detect decoding failures, retain malformed locations and count unknown versions separately. Test valid JSON with invalid schema and invalid UTF-8, not only torn JSON. |
| F21 | major | creation-clock-predates-item | Task 5 step 3 | Registration uses the file’s initial addition commit for every newly introduced item, even when that item was added months later. As a higher-ranked CLI plan-commit observation, it defeats the correct item-specific creation recovered by git backfill. | Plan 1264–1268, 1317–1329, 861–862. Spec §6.3 requires first pinned plan commit containing the item, not first commit creating the file. | Derive creation independently per new item against a pinned history, retaining source SHA, or use explicit --created-at/current time with its stated basis. Test an old plan file extended with a new task and compare registration with backfill. |
| F22 | major | missing-item-estimate-deltas | Tasks 7, 8 — output shape | M1 promises estimate-versus-actual per task, mission and campaign, but only aggregate task accuracy is emitted. Compact task rows contain estimate and clocks but no selected actual, ratio, inside/outside result, base or exclusion reason. Only non-task rows get schedule variance. | Plan 1962–1988, 2159–2161, 2234–2239. Spec §13 M1 Metrics explicitly requires per-item actual versus range; §6.11 requires metric units/basis/population and exclusions. | Add an explicit per-item comparison result to JSON and Markdown with selected original/latest estimate, fixed actual and basis, range hit, ratio, and exclusion reason; keep aggregate statistics derived from those results. |
| F23 | major | missing-strata-quality-denominators | Tasks 7, 8 — grouping/coverage | Metrics pool all task classes, known roles and estimate tiers within a level. --class and --level are silently ignored. Coverage tracks start basis rather than dispatched/first-commit/done shares by source. JSON has no quality population, and Markdown’s generic review-not-measured text omits the reviewed/eligible/done/unknown denominators for each speed/volume stratum. | Plan 1935–1938, 1962–1992, 2228–2232, 2276–2290. Spec D13/D14, §§6.4, 6.10–6.11, §13 M1 coverage; AC-6. | Implement the required known strata and filters, retaining unit/basis/class/tier separation and source provenance. Attach explicit unknown quality denominators in M1 to each matching speed/volume population; do not invent review success or per-person data. |
| F24 | major | report-provenance-not-read-bytes | Task 8 step 3 | The report re-reads event files to hash them after metrics were computed, and labels canonical plan hashes as source-file hashes. It omits profile hash, schema/adapter versions and event path:line evidence. Thus its hashes need not identify the bytes that produced its numbers, even within the allowed non-atomic snapshot model. | Plan 2173, 2185, 2193–2195; resolveObservations drops readRaw locations for active records at 543. Spec §6.11 requires hashing the buffers actually read, plan/profile hashes and metric evidence locators. | Carry hashes and path:line provenance from the initial reads through resolution and metrics. Keep canonical plan identity hash separate from saved-file byte hash, and include profile/schema/adapter metadata. No snapshot transaction or manifest is needed. |
| F25 | major | missing-honesty-caveats-gaps | Task 8 step 3; Task 12 doctor | Reports omit unconditional capability limitations such as acceptance: unauthenticated, writes: non-transactional, partial-update: possible, capture-loss: possible, shared-files: last-writer-wins and sync: best-effort. registration-gaps are never checked; orphan-plan events are filtered out before unregistered counting. Unknown losses therefore look like clean zero coverage. | Plan 2177–2198, 2246–2249, 3116–3131. Spec §§6.1, 6.3, 6.11, 8; AC-9/11/16. | Render relevant capability caveats unconditionally in both formats, and distinguish checked zero from unavailable counts. Compare readable catalogue and observations for detectable registration gaps, count orphan-plan records before selection, and surface capture/lock/sync diagnostics without promising recovery. |
| F26 | major | hook-roster-too-broad | Task 11 step 3 | Admission checks every installed Claude agent, not the union of the session plan’s participating factories. Installing manual-qa beside feature-development lets a manual-qa role’s matching task reference produce attributed delivery events. plan.roster is never consulted. | Plan 2843–2845, 2914–2920; registration stores only [full.factory] at 1322. Spec D16, §6.6; bundles/SPEC.md:291–296 and proposed P-1. | Persist or resolve the participating-factory roster union and check role membership in that union. Preserve the missing-type exception only for the role test. Add a foreign-but-installed role test and a legitimate multi-factory plan test. |
| F27 | major | missing-spike-worktree-proof | Task 0; Task 11 steps 1–3 | The required SPIKE-1 never occurs. Hook tests invent transcript shapes; no real pinned Agent/Workflow/concurrent/main/worktree fixtures are supplied. ownerRepo recognizes only a literal .claude/worktrees path, so an ordinary git worktree can write to the wrong telemetry owner or miss the registered plan. | Plan 2679, 2686, 2716–2723, 2834–2840; Task 0 has only documentation steps. Spec §13 M-1, §6.6, §12, AC-4/5. | Add the prerequisite probe and sanitized fixtures with source/version/hash provenance before implementation. Resolve linked worktrees through git metadata and test both Claude-created and arbitrary-path linked worktrees. Unsupported real shapes must remain unknown. |
| F28 | major | submodule-bootstrap-unapproved-deferral | Task 12 interface; Task 13 step 6 | The plan moves shared-submodule bootstrap to M2 and instructs the executor to amend the approved spec to match. This contradicts M1 storage §6.1 and makes delivery depend on tokenomics for setup. In plain-dir mode bestEffortSync simply returns without committing, despite the printed committed-record claim. | Plan 1419, 2979, 3110–3113, 3241. Spec D1/D3, §§6.1, 13 M1, AC-16; bundles/SPEC.md:299–304. | Implement/reuse the specified self-referential shared telemetry setup independently of tokenomics in M1. Remove the spec-rewrite instruction and plain-dir durability claim, unless a separate explicit scope change is approved before this plan is executed. |
| F29 | major | sync-never-integrates-remote | Task 5 step 3 — bestEffortSync | After another writer pushes, every subsequent invocation repeats push against the same divergent local branch. No fetch/merge/retry path exists, so retry-next-time cannot recover ordinary shared-branch advancement. Partial writes followed by an exception also bypass the sync request entirely. | Plan 1422–1425, 1435–1440. Spec §6.1 requires best-effort fetch/merge and one retry for rejected push, then retry next invocation; every delivery mutation requests sync. | Implement the bounded fetch/merge/push retry, stop and surface any merge conflict, and request sync after partial successful mutation as well as normal completion. Add offline-then-online and two-writer local-remote tests; retain the advisory guarantee ceiling. |
| F30 | major | inner-ignore-doctor-incomplete | Task 12 step 3 | Existing telemetry repositories without .gitignore never receive inner rules, so git add -A can stage reports and lock/transient files. Doctor checks only a main-tree lock path, not all inner/root patterns, tracked transients or disposable-index staging, and does not inspect dirty state, merge state, git locks or conflicted paths. | Plan 3103–3107, 3124–3129. Spec §6.1, §6.6; AC-2/16 explicitly require these checks. | Create the owned inner ignore block when the telemetry owner exists, even if its ignore file does not. Check all patterns in the owning repositories and disposable indexes; report tracked transients and interrupted/conflicted git state without modifying it. Test an actual telemetry submodule, not only a plain directory. |
| F31 | major | cli-exit-window-contract | Tasks 5, 8 — dispatcher/errors | Expected user errors fall through as INTERNAL exit 1: invalid at, malformed bulk JSON, makeObservation schema failures and LOCK-BUSY lack .code/.exit. NO-EVENTS checks all occurrences rather than those before end, so a future-only ledger reports success. --sha/--at disagreement remains rejected even with --revision. | Plan 1340–1353, 1362–1364, 1438–1440, 2182, 2286. Spec D21, §§6.5, 6.9; plan 24 and 2036. | Normalize known validation/lock/JSON/date errors to exit 2 with one-line codes; count readable observations before the effective end for NO-EVENTS. Implement the permitted explicit correction-clock path. Add invalid-input, busy-lock, future-only and corrected-sha CLI tests. |
| F32 | major | estimate-exclusions-band-contract | Task 7 step 4 | The schedule variance band is ordered actual-low then actual-high (56 vs 40–60 renders +16 / -4), opposite the specified lower-to-upper band. Zero midpoint silently suppresses work_ratio without a count; unknown-probability and missing-start reasons are absent. Latest mode mislabels an item with only late accepted estimates as unaccepted. | Plan 1963, 1967–1978, 1980–1982, 2239; test 1860 asserts the reversed band. Spec §6.4: actual-high to actual-low, explicit zero/point/probability/timeliness exclusions. | Emit ordered band bounds [-4,+16] for the fixture, add explicit denominator/exclusion counts, and preserve the reason latest selection found no eligible estimate. Do not relabel recorded acceptance as absent acceptance. |
| F33 | major | m1-scope-and-offline-gates | Tasks 8, 13; final self-review | The plan ships archived --from-json rendering from M4 while calling it M1, and its final gates omit test-automation installation and Copilot/Codex CLI smokes. The sole factory smoke may clone external skills; no offline cache setup is provided, and the npx fallback may fetch a validator. | Plan 2036, 2276–2288, 3243–3244, 3258. Spec §13 assigns --from-json to M4; §12 requires both factories and Claude/Copilot/Codex smokes with offline external cache. | Keep M1 scope explicit: defer archived rendering to M4 unless separately approved. Provide exact offline validation commands/cache fixtures and both factory plus cross-host CLI install smokes; resolve validator availability before the final gate rather than relying on network fallback. |

## Checked and fine

- The supplied pure UTC-week/year-boundary tests pass, including Jan 1, 2027 → 2026-W53 and an exact Monday end excluding the following week. The separate declared-coverage defect remains F15.
- The supplied duration stats tests pass: nearest-rank median, P85 and P90 use the specified 5/7/10 sample floors; n/min/max and low-n samples are retained.
- The hand calculations 50 h lead versus 2 h cycle, 56 h parent elapsed, ratio 1.12, 16 h turnaround and 120 h overlap are correct. The exclusion count and status-age exceptions are recorded above.
- User-name/OS/email precedence and slug normalization match `bundles/test-automation/skills/tokenomics/hooks/telemetry-capture.mjs:75–94`; percent/slash segment encoding round-trips without conflating a literal percent escape.
- The proposed implementation imports Node built-ins only, uses ESM, and does not import or modify tokenomics production code. CLI entry-point URL handling uses fileURLToPath.
- The ledger uses one appendFileSync per observation, accepts a complete JSON record at EOF, does not truncate torn tails, and uses only a delivery-local advisory mkdir lock with a 60-second stale rule.
- For valid active observations supplied directly, occurrence selection correctly prefers CLI over git and quarantines unequal equal-rank at/event/result/estimate facts. The separate revision handoff/equivalence defects remain F7/F8.
- M1 active_min actuals remain unavailable rather than being invented from elapsed time; missing dispatch is reported as commit_to_done rather than task cycle time.
- Proposed SKILL.md keys, quoted compatibility, name, metadata version and description length fit the stated frontmatter constraints; no tools key is introduced. Task 1 registers the orphan and both factories and invokes the marketplace generator.
- Installer hook entries are opt-in, async SubagentStop with a 30-second timeout; semantic removal preserves separate unrelated entries, including entries added later. Unsupported installer hosts return 2.
- The hook’s guarded runtime catches failures and routes diagnostics to delivery files rather than stdout; no SessionStart injection, Stop gate, Copilot capture or person leaderboard is added.
- PR-mode backfill, automation/cost joins, calibration and HTML are explicitly deferred. No transactions, manifests, fsync guarantees, process fencing or cross-writer locks are needed to fix these findings.

```json findings
{"version": 1, "verdict": "needs-changes", "counts": {"blocker": 19, "major": 14, "minor": 0}, "findings": [{"id": "F1", "severity": "blocker", "slug": "markdown-group-parser", "title": "The importer cannot pass its fixture: its fallback runs only when the primary regex finds no number", "sections": ["Task 4 steps 1, 3–4"]}, {"id": "F2", "severity": "blocker", "slug": "registration-retry-rejected", "title": "An identical registration retry exits 2 before reaching append/dedup", "sections": ["Task 5 steps 1, 3"]}, {"id": "F3", "severity": "blocker", "slug": "metrics-fixture-exclusion-count", "title": "The hand-computed test expects three unestimated exclusions, but the implementation skips clock-skewed t5 before examining its estimate", "sections": ["Task 7 steps 2, 4–5"]}, {"id": "F4", "severity": "blocker", "slug": "report-fixture-failures", "title": "Two report assertions cannot pass: default since is returned verbatim without milliseconds, and the open-item age is hand-calculated incorrectly", "sections": ["Task 8 steps 1, 3, 5"]}, {"id": "F5", "severity": "blocker", "slug": "backfill-test-existing-branch", "title": "The positive backfill test ends by creating task/task-002 even though its helper already created and retained that branch", "sections": ["Task 9 steps 1, 5"]}, {"id": "F6", "severity": "blocker", "slug": "golden-extractor-invalid-dataset", "title": "The supplied extractor does not produce the required dataset and its memberships cannot register", "sections": ["Task 10 steps 1–4"]}, {"id": "F7", "severity": "blocker", "slug": "revision-conflict-fallback", "title": "Revision conflict handling violates both supersession and no-fallback rules", "sections": ["Tasks 2, 6, 8 — resolution pipeline"]}, {"id": "F8", "severity": "blocker", "slug": "generation-semantic-identity", "title": "Different runs can collapse into the same observation, and materially different dispatch facts can be treated as equivalent", "sections": ["Tasks 2, 3, 6 — identity and equivalence"]}, {"id": "F9", "severity": "blocker", "slug": "recut-history-disconnected", "title": "A v2 plan only receives its delta events, but reporting v2 filters out every v1 observation", "sections": ["Tasks 3, 5, 8 — catalogue/replay"]}, {"id": "F10", "severity": "blocker", "slug": "invalid-stable-id-migration", "title": "Explicit duplicate item_id values are accepted, and deterministic slug assignment can collide even when refs are distinct", "sections": ["Tasks 3, 5 — validation and re-cut"]}, {"id": "F11", "severity": "blocker", "slug": "parent-integration-boundary", "title": "All-terminal children automatically complete missions/campaigns without integration evidence", "sections": ["Task 6 step 3"]}, {"id": "F12", "severity": "blocker", "slug": "invalid-transition-replay", "title": "The CLI never validates a transition against history", "sections": ["Tasks 5, 6 — event validation/replay"]}, {"id": "F13", "severity": "blocker", "slug": "rounding-before-accuracy", "title": "Durations are rounded to two decimal hours before ratio, hit-rate and MRE calculations", "sections": ["Task 7 step 4"]}, {"id": "F14", "severity": "blocker", "slug": "duration-validity-not-per-metric", "title": "The implementation excludes retrospective lead solely from short duration and plan-commit basis, without same-commit evidence", "sections": ["Task 7 step 4"]}, {"id": "F15", "severity": "blocker", "slug": "velocity-before-declared-coverage", "title": "Weekly series use the shared requested window without consulting each plan’s observation_start or full declared coverage", "sections": ["Tasks 7, 8 — window/throughput"]}, {"id": "F16", "severity": "blocker", "slug": "estimate-acceptance-reuse", "title": "A changed range or tier can reuse the old acceptance pair and count as timely human-accepted work", "sections": ["Tasks 3, 5 — estimate validation/delta"]}, {"id": "F17", "severity": "blocker", "slug": "hook-unvalidated-child-clocks", "title": "Any existing agent_transcript_path is trusted without checking host/session/agent correlation", "sections": ["Task 11 step 3"]}, {"id": "F18", "severity": "blocker", "slug": "backfill-wrong-completion-history", "title": "The adapter selects the newest qualifying merge as episode-1 because git log is reverse chronological and later matches are skipped", "sections": ["Task 9 step 3"]}, {"id": "F19", "severity": "blocker", "slug": "sync-stages-unresolved-conflicts", "title": "Blind git add -A followed by commit can stage unresolved conflict-marker files and commit them as a resolved merge", "sections": ["Task 5 step 3 — bestEffortSync"]}, {"id": "F20", "severity": "major", "slug": "ledger-schema-not-validated", "title": "readRaw accepts any v2 object with observation_id and an integer revision, including negative revisions, absent clocks, invented sources/events and invalid estimates", "sections": ["Task 2 step 3"]}, {"id": "F21", "severity": "major", "slug": "creation-clock-predates-item", "title": "Registration uses the file’s initial addition commit for every newly introduced item, even when that item was added months later", "sections": ["Task 5 step 3"]}, {"id": "F22", "severity": "major", "slug": "missing-item-estimate-deltas", "title": "M1 promises estimate-versus-actual per task, mission and campaign, but only aggregate task accuracy is emitted", "sections": ["Tasks 7, 8 — output shape"]}, {"id": "F23", "severity": "major", "slug": "missing-strata-quality-denominators", "title": "Metrics pool all task classes, known roles and estimate tiers within a level", "sections": ["Tasks 7, 8 — grouping/coverage"]}, {"id": "F24", "severity": "major", "slug": "report-provenance-not-read-bytes", "title": "The report re-reads event files to hash them after metrics were computed, and labels canonical plan hashes as source-file hashes", "sections": ["Task 8 step 3"]}, {"id": "F25", "severity": "major", "slug": "missing-honesty-caveats-gaps", "title": "Reports omit unconditional capability limitations such as acceptance: unauthenticated, writes: non-transactional, partial-update: possible, capture-loss: possible, shared-files: last-writer-wins and sync: best-effort", "sections": ["Task 8 step 3; Task 12 doctor"]}, {"id": "F26", "severity": "major", "slug": "hook-roster-too-broad", "title": "Admission checks every installed Claude agent, not the union of the session plan’s participating factories", "sections": ["Task 11 step 3"]}, {"id": "F27", "severity": "major", "slug": "missing-spike-worktree-proof", "title": "The required SPIKE-1 never occurs", "sections": ["Task 0; Task 11 steps 1–3"]}, {"id": "F28", "severity": "major", "slug": "submodule-bootstrap-unapproved-deferral", "title": "The plan moves shared-submodule bootstrap to M2 and instructs the executor to amend the approved spec to match", "sections": ["Task 12 interface; Task 13 step 6"]}, {"id": "F29", "severity": "major", "slug": "sync-never-integrates-remote", "title": "After another writer pushes, every subsequent invocation repeats push against the same divergent local branch", "sections": ["Task 5 step 3 — bestEffortSync"]}, {"id": "F30", "severity": "major", "slug": "inner-ignore-doctor-incomplete", "title": "Existing telemetry repositories without .gitignore never receive inner rules, so git add -A can stage reports and lock/transient files", "sections": ["Task 12 step 3"]}, {"id": "F31", "severity": "major", "slug": "cli-exit-window-contract", "title": "Expected user errors fall through as INTERNAL exit 1: invalid at, malformed bulk JSON, makeObservation schema failures and LOCK-BUSY lack .code/.exit", "sections": ["Tasks 5, 8 — dispatcher/errors"]}, {"id": "F32", "severity": "major", "slug": "estimate-exclusions-band-contract", "title": "The schedule variance band is ordered actual-low then actual-high (56 vs 40–60 renders +16 / -4), opposite the specified lower-to-upper band", "sections": ["Task 7 step 4"]}, {"id": "F33", "severity": "major", "slug": "m1-scope-and-offline-gates", "title": "The plan ships archived --from-json rendering from M4 while calling it M1, and its final gates omit test-automation installation and Copilot/Codex CLI smokes", "sections": ["Tasks 8, 13; final self-review"]}]}
```
