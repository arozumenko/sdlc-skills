# delivery-metrics M1 plan v2 — adversarial review (codex gpt-6-astra, round 2)

Date: 2026-09-17.

## Verdict

needs-changes

1 blocker / 3 majors / 0 minors

V2 repairs many first-round defects, but registration still changes a specified clock rule, and installed roster discovery, shared worktree ownership and historical parent reduction require additional design decisions.

Review target: the 710-line v2 at commit `2d3df0a0d5be3171161a41bb6b9fd28fb4827f3e`, plan SHA-256 `f9b9dcd449190345fb112f6ed97c1889b659f0506a240feae1c4d77d66458f36`. During review, commit `b41dcc3` reverted the working-tree file to v1. All Plan line references below identify the pinned v2, not that reverted file. V2 mostly provides implementation/test instructions rather than complete executable modules; no future implementation suite is claimed to pass.

## Findings

| id | severity | slug | task/step | finding | evidence | concrete change |
|---|---|---|---|---|---|---|
| F1 | blocker | creation-clock-predates-item | Task 5 steps 1–2 | **Registration still assigns the wrong default creation clock; the repair changes the approved precedence.** For an item demonstrably present in pinned commit B, registration now always emits observed registration time unless --created-at is supplied. Without a later, separate backfill, lead time and creation-cohort figures use registration time instead of B. This is an explicit contract change, not merely an honesty label. | Plan 329, 352; Spec §6.3 requires explicit created-at, else pinned first containing commit, else now. The resolution table at Plan 698 calls this resolved by contract although the spec was not changed. | Resolve each item’s pinned first-containing commit during registration, falling back to now only when unavailable. Share the Task 9 resolver without introducing a CLI dependency cycle. Test a historical committed plan registered later, with no backfill, including an item introduced in a later commit. |
| F2 | major | installed-roster-unavailable | Tasks 5 step 2, 11 step 2, 13 step 3 | **The installed skill has no defined source for participating-factory membership.** Registration requires installed factory metadata or a verified roster snapshot; the hook additionally requires installed participating-factory role metadata even when a snapshot was supplied. Neither task ships that metadata or defines the snapshot schema/verification. Installed agents and the global role defaults establish which roles are installed, not which participating factory owns them. A fresh engineer must invent this mechanism to make ordinary registration and positive capture work. | Plan 352, 568, 628, 634–647; bin/init.mjs:202 loads factory.json from PKG_ROOT; :2726–2803 copies agents/skills rather than factory manifests; :871–887 generates defaults from all installed agents. Spec §6.6 requires the participating-factory union, not all installed roles. | Ship a concrete factory-to-role map with the orphan skill, or explicitly add/install the required metadata. Define snapshot shape and validation plus how the hook consumes it. Exercise the six installed CLI smokes and a multi-factory/foreign-role hook fixture without access to the source checkout. |
| F3 | major | missing-spike-worktree-proof | Tasks 1–5 and 11 step 2 | **Only the hook resolves the shared telemetry owner; CLI writes can land in a different worktree.** The shared path APIs take repo directly and the CLI takes repo without an owner-resolution step. Task 11 alone introduces ownerRepo and redirects reads/writes to the main checkout. Running register/session set from an arbitrary linked worktree can therefore write a local delivery directory while its completion hook looks for the guard and plan in main. Hook-only ownership tests do not cover this cross-entry-point failure. | Plan 114, 128–129, 323, 352–354 versus 558, 566–568; Spec §§6.1/6.6 and AC-4/5. The dependency order also places the hook-owned resolver after CLI/report/backfill. | Move owner resolution to a shared early module and specify its use by CLI, hook, installer and doctor. Add an end-to-end fixture that registers and binds from an arbitrary linked worktree, captures its child, and reports the same ledger from main and that worktree. |
| F4 | major | parent-integration-boundary | Task 6 step 3; Task 7 step 2 | **Endpoint-only parent reduction does not define how to preserve an earlier qualified completion.** The reduction uses required endpoint children and leaves a parent open when any is nonterminal, but the following paragraph requires preserving first completion after scope addition. Example: v1 children finish and land Monday; v2 adds an unfinished child Tuesday. At Wednesday’s cutoff, the specified reduction cannot recover Monday’s qualification from endpoint children. No historical parent-reduction pass or distinct first-completion/current-scope fields are defined. Reusing the sole terminal field risks either losing historical throughput/accuracy or reporting pending scope as delivered. | Plan 77, 239, 397–410, 446; Spec §5 says scope addition must not erase first completion; §§6.4/6.9 require first-completion actual and throughput. Task 6’s listed fixtures at 414 do not exercise completed parent followed by scope addition. | Specify an effective-history reduction that records the first qualified parent start/finish using membership and landing evidence at that boundary, then separately derives endpoint pending scope. Define both results in Item and metric selection. Add the Monday/Tuesday example and assert Monday throughput/actual remain fixed while Tuesday’s added work remains pending. |

## Checked and fine

- Executed v2’s actual parseGroups function in memory under Node v24.14.1 against the pinned security-testing source: 30 groups / 59 memberships; G0, G2 and G6 match the specified corrected memberships.
- Independently hashed the pinned source bytes: `457787ff3b7ba23e193d213db98247042cbdb5c5e9f56c7f922de4cf23a5af4f`, matching Task 10.
- The supplied stats snippet uses nearest rank and the stated 5/7/10 percentile floors; velocity explicitly uses its separate three-whole-week rule.
- Recomputed 50 h lead, 2 h cycle, 255 h open age, 15/3600 h subminute actual, and parent 56/50 ratio with ordered [-4,+16] band; these arithmetic corrections are sound.
- V2 explicitly specifies maximum-revision conflict recovery, rank blockers without fallback, generation-qualified identities and preserved evidence; the previous contradictory v1 implementations are removed.
- Registration retries now retain request observations and recognize identical requests before stale-version rejection; partial append retries and finally-sync are explicitly covered.
- The sync recipe checks unresolved Git state before staging and stops on merge failure; it allows only bounded best-effort retry and does not promise transactions or cross-writer exclusion.
- The supplied SKILL.md frontmatter fits the repository’s strict scalar/no-tools conventions; orphan and both factory registration plus marketplace generation are specified.
- The offline installer recipe accounts for the installer’s unconditional cache fetch using local mirrors, URL rewrites and file-only protocol; the cache layout matches bin/init.mjs:1140–1175.
- Real hook probes, unsupported-shape handling, unconditional caveats, read-byte hashes, unknown loss totals, and deferred PR/review/reopen/cost/archive capabilities are explicit. These are plan requirements, not evidence that the future fixtures or implementation already exist.

```json findings
{"version": 2, "verdict": "needs-changes", "counts": {"blocker": 1, "major": 3, "minor": 0}, "findings": [{"id": "F1", "severity": "blocker", "slug": "creation-clock-predates-item", "title": "Registration still assigns the wrong default creation clock; the repair changes the approved precedence.", "sections": ["Task 5 steps 1–2"]}, {"id": "F2", "severity": "major", "slug": "installed-roster-unavailable", "title": "The installed skill has no defined source for participating-factory membership.", "sections": ["Tasks 5 step 2, 11 step 2, 13 step 3"]}, {"id": "F3", "severity": "major", "slug": "missing-spike-worktree-proof", "title": "Only the hook resolves the shared telemetry owner; CLI writes can land in a different worktree.", "sections": ["Tasks 1–5 and 11 step 2"]}, {"id": "F4", "severity": "major", "slug": "parent-integration-boundary", "title": "Endpoint-only parent reduction does not define how to preserve an earlier qualified completion.", "sections": ["Task 6 step 3; Task 7 step 2"]}]}
```
