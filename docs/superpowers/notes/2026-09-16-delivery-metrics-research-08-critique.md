# 08 — Completeness critique of research reports 01–07 (delivery performance tracker spec)

Scope: the seven reports in this directory, read in full. `NN:L` = report `NN-*.md` line L. Repo
paths are relative to `/Users/Daniel_Sallai/dev/sdlc-skills` (branch `feat/security-testing-bundle-spec`,
HEAD `b1f70d2`, 2026-09-16). Where I ran a lookup to settle a point, the result is marked **verified**
with the source file:line. Nothing here is a design; it is what the spec author must decide or respect.

---

## 1. Contradictions between reports

| # | Topic | What the reports say | Verdict / what the spec must do |
|---|---|---|---|
| C1 | **Does "mission" exist in the repo?** | 03:18-19 "`mission` does not exist anywhere in this repo (`grep -rl mission` only hits the substring in 'permission')"; 04:445 "the word **'mission'** does not occur anywhere in the repo (grep verified)". 02:46 finds it in `bundles/feature-development/instructions.md:90,94-95` and `agents/qa-engineer/AGENT.md:95,99`. | **Verified: 02 is right, 03 and 04 are wrong.** `grep -rnwI mission bundles skills` hits 17 lines: `feature-development/instructions.md:90`, `qa-engineer/AGENT.md:95,99`, `manual-qa/instructions.md:76`, `product-management/instructions.md:94`, `skills/memory/SKILL.md:136,187`, `skills/knowledge-curation/SKILL.md:3,29,85,91`, `knowledge-curation/README.md:14`. It is always loose prose ("once this mission ends", "mission state belongs on the work board"), never an id or artefact. The spec must define "mission" or not use it; 03:40 and 04:447 still stand on the substantive point (no record to join to). |
| C2 | **Definition of lead time** | 05:67 vault tooling: `first appearance → first done`, hours, explicitly "board-tracked duration, not idea-to-done" (05:82, 05:139). 07:227 vendor consensus: `creation → done`. 06:111 Kanban community: `request/arrival → delivered` (customer clock); DORA: `commit → production`. 02:275 offers `created` = plan commit, which for the dataset is one timestamp for all 59 tasks (02:269). | Three different start events (item created / request arrived / commit) and the harness's only "created" is a batch stamp. The spec must pick ONE start event for `lead_time`, name it in the label (05:219 "say which clock it is"), and say that DORA lead time is out of scope (06:316). |
| C3 | **Cycle-time start event** | 06:316 "*started* = dispatch of the item to an agent"; 07:249 "first dispatch whose `label` matches the item"; 07:228 vendors: In-Progress category OR first commit, configurable; 02:233 `branch created → first commit` as effort proxy; 03:566 batch start = earliest `scopes[].declaredAt`; 05:220 first `executing`. Meanwhile 02:260 "dispatch time … no commit; branch creation is only in the local reflog", 01:177-178 dispatch record "has **no `startedAt`**", 01:624-625 "nothing today records dispatch **start** times". | The industry-preferred start (dispatch) is exactly the event the harness cannot durably record today. The spec must either (a) add a dispatch-start capture (hook on `SubagentStart`/`PreToolUse Agent`, cf. 01:68 `--mark-dispatch` precedent) or (b) declare a proxy start (first commit / `declaredAt`) and label it a proxy (05:247). |
| C4 | **Re-entry / backward-transition rule** | 07:206 "derived … by 'first entry into a state category' … **cleared on backward transition** (ADO)"; 07:249 "State the interval rule (**sum**, like Swarmia)"; 07:229 admits vendors split (sum of intervals vs first entry). | Report 07 recommends both. Spec must pick one per metric (e.g. cycle time = first `started` → first `done`; `time_in_state` = sum of intervals) and say what a FAIL review round does to "started". |
| C5 | **The "done" event in test-automation** | 06:316 finished = "merge of the item's branch (or the case's PASS receipt)"; 03:565 wave finished = `gate-runs.jsonl` `{verdict: green, at}` "most reliable 'done' clock today"; 03:552 "script-authored records … and git (merge time) are the trustworthy signals"; 03:393-395 receipt `outcome` "known to lag reality … 55% misrecorded". 02:127 feature-dev done = merge commit (batched per group, 02:234). | Four candidate clocks (receipt, scope `outcomes[id].at`, gate `at`, git merge). Spec must rank them per level (case / wave / task) and record which one produced each `done` (raw beside canonical, 07:246). |
| C6 | **What "actual" means** | 03:232 "metered active agent-minutes and dollars … *not* calendar time"; 06:290/07:228 elapsed calendar time; 05:201 "the only 'actual' is AI spend and `net_loc`"; 02:233 `created → first commit` ≈ "pure implementation". | Four actuals. The spec must name at least two distinct columns (`active_min` from tokenomics vs `cycle_time` from this tracker) and never put both under one word (05:244 "two denominators … never quoted interchangeably"). |
| C7 | **Estimate: point, range, or none** | 03:52 "**The one number this skill will never produce is a bare point estimate**"; 06:317 estimate accuracy needs a point (`actual/estimate`, MdMRE, PRED(25)); 02:92 devs "**Don't give time estimates**"; 05:200 "**AI does not estimate** — it suggests ranges"; 07:232 Linear "unestimated issues count as 1 point" vs 05:240 "Never estimate … absent = not measured, never 0". | Spec must define `estimate` as a *recorded declared input* (may be ordinal S/M/L, or a range with a tier), compute point statistics only on the midpoint/`estMin` of a range and say so, compute interval hit-rate where a range exists, and forbid the "unestimated = 1" default (repo ethos wins, 04:431). |
| C8 | **Is the tokenomics est-vs-actual join working?** | 03:296-302 `loadSizings` reads `estimated_active_minutes ?? est_min` but `score-cases.mjs --json` emits `estMin` → production `estVsActualMin` silently omitted. 05:205 cites the same feature as if functional ("already emits … from `.agents/estimation/` (`estimated_active_minutes`…)"). 01:499-501 lists both accepted keys without flagging the mismatch. | **Verified:** `batch-cost.mjs:344` `estMin: num(r.estimated_active_minutes ?? r.est_min)`; `score-cases.mjs:466` `estMin: round1(estMin)`. 03 is right. Spec: fix in tokenomics (M-1) or accept both keys — decide, do not inherit silently. |
| C9 | **Zero / sub-minute durations** | 05:69,140 exclude `lead_h ≤ 0.02 h` (72 s) from duration stats, keep in throughput; 06:117 Vacanti counts `finish − start + 1` days so minimum is 1, never 0; 01:562 tokenomics rounds to whole minutes at unit level "so sub-minute dispatches round to 0". | Spec must pick a resolution (seconds in the ledger, minutes/hours in reports) and an exclusion threshold with a printed count (05:221). |
| C10 | **Percentile and small-n policy** | 05:72 p90 nearest-rank `sorted(v)[int(0.9*(n-1))]`; 06:290 "median and **P85**"; 01:505-507 tokenomics flags on p90/p10 with `MIN_CLASS_N = 5`; 06:282 "P85 needs ≥ 20 … below the floor report min–max and n"; 05:71 velocity `MIN_WHOLE_WEEKS = 3`. | Vault (p90) vs industry (P85) vs tokenomics (p90/p10, n≥5). Spec must fix the percentile set, the rank method, and per-statistic n floors. |
| C11 | **Must skill-installed hooks roster-guard?** | 01:126-129 tokenomics' `SubagentStop` matcher `*` "is **not** roster-guarded; it is acceptable because it is repo-level opt-in"; 04:192-197 and 04:415 "**Any** hook on a shared event must roster-guard and fail open (`bundles/SPEC.md:290-296`)". | SPEC.md:290-296 (verified) says "a hook that fires on a generic event … runs in EVERY factory's sessions … must check … roster". A cross-factory tracker has *no* single roster — the spec must say what its guard is (e.g. presence of its own scope/id catalogue) rather than inherit tokenomics' exemption. |
| C12 | **Where the skill lives and which hierarchy it serves** | 01 (title, §10) assumes a sibling of tokenomics inside `test-automation`; 02:180 assumes `.agents/telemetry/<area>/` by analogy for feature-dev; 04:389-395 says "pick one and say why" (orphan vs factory copies); 07:5,247 assumes a `campaign / mission / task / case` hierarchy that 03:40 says has no referent. SPEC.md:298-304 "**one subfolder per factory**" vs 01:616 "one subfolder per factory/concern". | The task brief's hierarchy is not the repo's. Spec must (a) decide orphan vs factory-owned, (b) name the telemetry subfolder(s) and reconcile with the "per factory" wording (a single cross-factory `delivery/` subfolder is a SPEC amendment; per-factory subfolders written by one skill is not). |
| C13 | **Portable source of truth** | 02:286 "A git-only derivation … is the only source that works on every host and on every clone; reflog- and transcript-based sources are machine-local and expiring"; 07:243 "**Store events, derive timestamps** … one line per transition" (needs hooks); 04:163 factory hooks are Claude-only v1; 01:82-95 skill-installed hooks reach Copilot too. | Two architectures (derive from git vs event ledger via hooks). Spec must state the primary and the fallback and the provenance label each produces. |
| C14 | **"batch"** | 03:26 "flat batch (no campaign)" = a wave without a campaign; 01:225 cost.json `batch` slug = receipt dir path incl. nested `camp/wave-01`; 01:405 batch window from scopes whose `batch` equals slug; 02:49 feature-dev batching word is "group / phase" (`G0..G29`). | One word, three referents. Glossary below fixes it. |
| C15 | **`work_item_level` default** | 01:254-255 template says `"batch"`, code default `'feature'`; 07:247 quotes only `'feature'`. | **Verified:** `factory-profile.template.json:8` `"batch"`, `build-tokenomics-export.mjs:66,105` `?? 'feature'`. Spec's level names must map onto this field or fix it. |
| C16 | **"cadence"** | 05:207 vault: review/ritual cadence, never a throughput metric; 07:234 DORA-shaped "deployment frequency"; 06:271 throughput count per week; the task brief uses it as a delivery metric. | Spec must define `cadence` once (see glossary) or drop the word. |
| C17 | **"rework"** | 07:231 three vendor senses; 01:230 tokenomics `rework` = fix-round dispatch spend (`FIX_STAGE`); 03:221 `reworkSignal` regex on case note; 02:133 feature-dev = count of `TASK-NNN: address review N` commits. | Four senses. Glossary picks `review_rounds` (count) and leaves `rework` to tokenomics (spend). |
| C18 | **Per-person data** | 05:246 / 06:319 "No per-person scoring in a tracked/shared output"; 01:140 ledger is per-user (`usage-<user>.jsonl`), 01:378 team-report has `byPerson`; 07:252 "the line should still carry `user`". | Not a true contradiction but must be stated: per-user files and `user` on every line are storage/attribution mechanics; the *report* aggregates to team (05:246). |
| C19 | **Merge detection** | 02:127 merge = `merge task/task-NNN (Rio: PASS …)` commit subject; 02:285-286 says both modes must be covered and git-only works "on every clone". PM's documented merge is `gh pr merge <N> --squash --delete-branch` (02:112 quotes `pm/AGENT.md:95`). | **Verified:** `project-manager/AGENT.md:95` squash + delete; `:232` "default to `auto-merge` + `squash`". Under squash there is **no merge commit and no branch** — the git-only parser of 02 only works in the observed local-branch mode. 02 contradicts itself; spec needs a PR-mode source (`gh pr view --json mergedAt`, 02:264) and a local-mode source. |
| C20 | **Is the task id available to a hook at dispatch?** | 02:145 "the `TASK-NNN` id is not available to it [agent-start]"; 01:611-613 "Task-tool dispatches have a real `description`. Workflow-tool sub-agents get no description (meta carries only agentType)"; 02:149 `.meta.json` = `{ agentType, description, toolUseId }`. | **Verified:** `telemetry-capture.mjs:606-613`; `workflow-return.mjs:169` reads `meta.description`. So the id *is* recoverable for Agent-tool dispatches (from the sidecar/description) and not for Workflow dispatches (the observed dataset was a Workflow, 02:122). 02 is too pessimistic; the spec must handle both dispatch kinds. |
| C21 | **Test-file placement** | 04:202-205: factory hooks keep `*.test.mjs` outside `scripts/` so they are not installed; tokenomics keeps them beside scripts and they *are* copied into consumer projects. | Two precedents; spec must pick. |
| C22 | **Outcome word `deferred`** | 07:246 "the open `outcomes` vocabulary in `work-scope.mjs` (`delivered`, `deferred`…)"; 03:375-390 lists the seven receipt outcomes; 01:607 outcomes are open. | **Verified:** `grep deferred work-scope.mjs scope-hook.mjs SKILL.md` → no hits. 07 invented `deferred`. Use 03's list. |
| C23 | **Unit of time** | 01:562 minutes (rounded); 05:67 hours; 03:487-489 manual-qa milliseconds; 06:117 days. | Spec must fix ledger unit (ISO timestamps; derive) and report unit per metric. |

---

## 2. Gaps — questions no report answers (lookups done where cheap)

**Placement, ownership, storage**

- **G1. Orphan top-level skill or factory-owned copy?** 04:389 lists the two routes but does not choose. Facts: `feature-development/factory.json` `skills: ["memory","knowledge-curation"]`, `targets: ["claude"]`, no `hooks` key (**verified** lines 163-169; 02:147); `test-automation/factory.json:11-14` same two orphan skills; tokenomics is `localSkills` in test-automation only (04:55). The `memory` lesson (04:66-68) argues orphan; the hook/submodule machinery argues importing from tokenomics, which is only installed with test-automation — **an orphan skill cannot `import` tokenomics' exported functions on a feature-development-only install** (01:596-605 "import, don't copy" assumes co-installation). Unanswered: does the tracker depend on tokenomics being installed, vendor its own copies (then `check-skill-dupes` GROUPS, 04:117), or move the shared helpers into an orphan skill both use?
- **G2. Telemetry subfolder name.** SPEC.md:298-304 (**verified**) says "one subfolder per factory". Options: one `delivery/` subfolder (needs a SPEC amendment), or `<factory>/delivery/` (but `automation/` is tokenomics' namespace, 01:643-645 — `ledgerFilesOf` only matches `^usage.*\.jsonl$` directly under `automation/` and `listScopes` only `automation/scopes/*.json`, so a nested `automation/delivery/` would *not* be misread, but the naming space is still claimed). Unanswered.
- **G3. Root `.gitignore` block.** 01:292-296 tokenomics' managed block lists only `automation/` paths; the submodule's inner `.gitignore` is generic (`*/live/`, `*/scopes/.pending-*` …, 01:290-291). What transient files will the tracker have, and does it need its own managed block for the plain-dir fallback (01:619)?
- **G4. Ledger growth/rotation.** No report covers retention; tokenomics has none (append-only per user). Question: one file per user, per user-per-month, or per item family?

**Identity and joins**

- **G5. Unique work-item key across time.** 03:468-470 case ids repeat across batch generations (receipt dir path is unique); 02:276 `TASK-NNN` is unique only within one plan file; 07:244 wants an idempotency key per event. Unanswered: the key shape (`<factory>/<plan-or-campaign>/<id>`?) and what happens when a plan is re-cut (`tasks v2`, 02:57).
- **G6. Can a git commit be joined to a ledger session?** Nobody asks. **Verified:** commits carry `Claude-Session: https://claude.ai/code/session_01LGhRqjVhKWmJu7Ki5bCH6Q` (`git log -3 --format=%(trailers:key=Claude-Session)`), while the ledger `id` is the local transcript stem, a UUID (`~/.claude/projects/-Users-Daniel-Sallai-dev-sdlc-skills/d86668a3-c566-4d99-a3a1-da49da83fe50.jsonl`), and `telemetry-capture.mjs` never reads `session_`/`claude.ai/code` (grep: no hits). **The trailer is not a join key to the ledger.** If cost-per-task (05:232) is wanted, the join must go through dispatch `label`/`cases[]`/`scope` (07:251), which for feature-dev means teaching tokenomics' `matchIds` an id catalogue (`TASK-NNN` list) — see G8.
- **G7. Which dispatch kind is in play?** Agent-tool dispatches carry `description` (id recoverable); Workflow dispatches do not (C20). The observed 33-task dataset was a Workflow. Unanswered: does the spec require the orchestrator to put the id in the first stage line (as test-automation's prompts do, `telemetry-capture.mjs:609-612`), and who enforces it?
- **G8. Does tokenomics' label classifier apply to feature-dev?** **Verified:** `STAGE_MARKER` (`telemetry-capture.mjs:613`) = `triage|analyst|implement\w*|combined|review\w*|fix round|carve|merge|hardening gate|mini-gate|gate for batch|report writer|write the report|diagnostician|stabiliz\w+` — `implement`, `review`, `merge`, `fix round` would fire on feature-dev prompts, but `matchIds` (`batch-cost.mjs:230`) needs receipt ids from `.agents/automation/**/report.json`, which feature-dev never writes. Unanswered: will the tracker publish an id catalogue tokenomics can consume, or will cost-per-task stay out of scope?
- **G9. Worktree vs project dir.** 02:279 suggests `git branch --show-current` inside the worktree yields `task/task-NNN`; hooks resolve the repo from `CLAUDE_PROJECT_DIR`/cwd (01:74). Whether `CLAUDE_PROJECT_DIR` points at the worktree (`.claude/worktrees/wf_…`) or the main checkout during a Workflow dispatch is not established by any report. Must be probed (cf. TASK-055 "Hook-input probe result", `tasks-v2.md:424`).

**Events and clocks**

- **G10. Dispatch start capture.** C3. Which event (`SubagentStart` core hook is read-only, 02:142; tokenomics `PreToolUse Agent|Workflow --mark-dispatch` touches a marker, 01:68) and which host encodings. Copilot has `subagentStart` (01:92); Cursor's is permission-only, Kiro's carries no agent name (04:187-188). Unanswered: v1 host list.
- **G11. Merge under squash policy.** C19. **Verified** `pm/AGENT.md:95,229-243`: squash + delete-branch default, strategy read from `.agents/profile.md § Automation PR policy` (`templates.md:254-268`, **verified**). Unanswered: PR-mode source (`gh pr view --json createdAt,mergedAt,reviews`, 02:264) vs local-mode merge-subject parse; how `Closes #N` in a squash commit maps back to `TASK-NNN` (the PR title convention is `<type>: <description> (#<issue>)`, 02:72 — no `TASK-NNN` in it).
- **G12. Review rounds source.** 02:133 honest signal = count of `address review N` commits (local mode); PR mode = `reviews[]` with `CHANGES_REQUESTED` (02:109); tokenomics = `fix round` dispatches (01:230). Unanswered: which is canonical per mode and whether a FAIL round emits `review_returned` (07:223).
- **G13. Abandoned / blocked / re-dispatched.** 02:114 "no documented state or artefact", 02:265-266 heuristics only. Unanswered: what event marks `cancelled`, whether `blocked` intervals are subtracted from cycle time (Swarmia sums active intervals, 07:54) or reported as `time_in_state`.
- **G14. Group (`Gn`) membership as data.** Only in plan markdown (`tasks-v2.md` §1) and revised in the PM log (02:69). 02:283 wants it recorded to separate queue wait from review time. Unanswered: derived (same-second branch creation, 02:124) or declared (an event).
- **G15. Wave / campaign start and end in test-automation.** 03:458-461: no wave start/end, campaign only in hand-written card Log lines, conductor return not persisted (03:398-403). Unanswered: require `work-scope open --batch <wave>` as the start (03:566) and gate green as the end (03:565), or add a structured Log to the card template.
- **G16. Backfill from history.** 05:178 treats git-recovered transitions as measurement; 02:260 reflog expires (90 d) and is per-clone; 02:149 transcripts expire (~30 d). Unanswered: is a `--backfill` command in scope, and what provenance label do backfilled events carry (05:248 wants `path, git ref/SHA, is_working_tree, generated_at`).
- **G17. Time zone and week policy.** **Verified:** `team-report.mjs:106-114` `isoWeek` uses *local* `getFullYear/getDay`; `filterWindow` uses local calendar date of `startedAt` (01:372); vault keys `%G-W%V` (05:70); git dates are `+0200`. No report picks UTC vs local or the `--cutoff` semantics (05:78). Must be pinned.

**Estimates**

- **G18. Estimate source for feature-dev.** Only `**Complexity:** S|M|L` in the plan header (**verified** `tasks-v2.md:408` `**Story:** US-001 · **Assigned:** … · **Depends on:** none · **Complexity:** S`); tracker template spells it `small/medium/large` (02:87). Unanswered: parse the markdown (02:263 "fragile"), add an `estimated` CLI the tech-lead runs, or amend `tech-lead/AGENT.md:187` to emit it.
- **G19. Which of the four "S/M/L" scales is which.** Story `Size` by AC count (02:86); task `Complexity` undefined (02:87); manual-qa `size` = execution wall-clock 1-3/3-8/8+ min (03:443); test-automation `XS…XL` → SP 1/2/4/8/16 (03:64) vs Linear 1/2/3/5/8 (07:132) vs tokenomics `size_tshirt` bands from `effort_days` (01:486) — note the round trip does not hold: one `M` case = 4 SP = 0.5 d → band `S`. Unanswered: whether ordinal classes stay ordinal (reference classes, 06:320) or get a numeric map, and which map.
- **G20. Estimate date.** 03:462-464 no intake timestamp anywhere. Unanswered: `estimated` event `at` = when the sizing file was written (mtime, fragile) or when the tracker first sees it.
- **G21. Interval hit rate.** 06:317(g) needs a range; only `automation-scoping` produces `lowMin/highMin` with a tier (03:113,134); feature-dev has none. Unanswered: is the hit-rate metric test-automation-only in v1?
- **G22. Baseline slot.** 05:228 each outcome metric needs a named, empty `baseline` field the tracker cannot fill. Unanswered: where it lives (`factory-profile.json` has no such key, 01:253-258).

**Reporting and consumers**

- **G23. Who reads the report and where it surfaces.** PM status format has no time fields (02:96, `pm/AGENT.md:399-416`); scout/test-automation-lead read tokenomics on demand (04:57); the AI-maturity assessor wants names grouped Flow Time / Throughput / Quality (05:37,55). Unanswered: does the spec amend the PM status template, ship an assessor-facing export, or both; does anything get injected (02:178 says the ledger must not, byte cap).
- **G24. Population / denominator for feature-dev.** 05:137 totals from disk, timestamps from history. Unanswered: what is "disk state" for a plan-file harness (the `#### TASK-NNN` headings? 59 in the dataset, 02:94) and how re-cut plans are reconciled (05:74 `reconcile()` precedent).
- **G25. Flow efficiency.** 06:95 defensible only if active time = agent session wall-clock inside the cycle; ledger `activeMin` uses `IDLE_GAP_MS = 30 min` (01:556). Unanswered: in or out of v1.
- **G26. Multi-item sessions.** G6 dispatched seven tasks in 15 s from one session (02:124,245). Unanswered: how session-level events (session start/end) attribute to items, or whether only dispatch-level events carry item ids (tokenomics splits `1/div`, 01:411-413).
- **G27. Exit codes and CLI surface.** 04:258-260 newest precedent `0/1/2/3/4/5`; tokenomics uses none of that. Unanswered: adopt or not.
- **G28. Docs drift to fix in M-1.** **Answered (04:23-28):** CLAUDE.md/README/AGENTS say 8 orphan skills and 28 externals; tree has 11 and 30. If the tracker is orphan #12, fix in the same milestone (04:408).
- **G29. Which hosts in v1.** Core hooks: Claude/Codex/Copilot (02:286); factory hooks Claude-only (04:163); tokenomics hooks Claude + Copilot (+ VS Code opt-in) (01:56-108). Unanswered.
- **G30. Does anything in tokenomics need to change?** C8 (`estMin` key), C15 (`work_item_level`), 01:259-261 (`factory-profile` path comment), 01:623-625 (dispatch `startTs` computed but dropped at `captureDispatches` L438-449 — a one-line addition would give dispatch start for free). Unanswered: which are M-1 amendments to tokenomics vs accommodations in the tracker.

---

## 3. Top 10 hard constraints the spec must respect

| # | Constraint | Traced to |
|---|---|---|
| H1 | **Stdlib ESM `.mjs`, Node ≥ 18, zero dependencies, offline `node --test`; optional tools only via `npx` with an honest `null`.** | 04:232-251; 01:545-550 (every file header "STDLIB ONLY"); CLAUDE.md:133-135 via 04:232 |
| H2 | **Never estimate, never default, never blend: `null` ≠ 0, absent = "not measured"; every allocation/proxy labelled as such; caveats derived from the numbers they guard.** | 01:423-440 (exact strings); 05:240-249 (all ten rules); 04:431-435; 05:137-146 traps 1,4,8 |
| H3 | **Durable records only under `.agents/telemetry/<subfolder>/` on the one shared self-referential `telemetry` submodule — never a second submodule or branch, never gitignore `.agents/telemetry`, never write inside `automation/`, never rewrite a tracked main-tree file from a hook, never put the ledger in injected docs.** | SPEC.md:298-304 (verified) via 04:210-216; 01:279-311, 01:616-619, 01:643-645; 01:241-243 (cost.json dirtied the tree); 02:178,284 (byte cap) |
| H4 | **Hooks: opt-in via the skill's own install script with its own marker key, own `.github/hooks/<name>.json`, own gitignore block markers; never stdout except the one sync announce line; never exit non-zero; `async: true` + `timeout`; bounded stdin read; roster-guard (or equivalent) on shared events; `--remove` and `--doctor` idempotent.** | 01:345-358; 01:548-550; 04:192-201, 04:412-417; SPEC.md:290-296 (verified); 02:146 (three rules) |
| H5 | **Do not re-implement capture, metering, store parsing, sweep, submodule sync or the dedup rules — import tokenomics' exported functions (or resolve G1 explicitly) and join on `session` + `agentId` + `label` + `cases[]` + `scope.batch`; quote cost.json figures with their existing honesty labels, never recompute dollars.** | 01:635-642, 01:596-605; 07:251; 05:232 |
| H6 | **Every metric declares start event, stop event, unit, `n`, source path/command, denominator and (outcome metrics) a baseline slot; durations as median/percentiles never means; percentile only when n supports it, else min–max + n; proxies named as proxies (PR time-to-merge ≠ lead time ≠ cycle time).** | 06:316-318; 06:282; 05:219-228; 05:90,96 (LABEL string); 07:229; 04:433-435 |
| H7 | **Segment, never rank: no per-person or per-agent productivity scores in a shared output; no speed/volume figure without its paired quality figure; item counts for throughput, never points.** | 05:246, 05:245, 05:159; 06:175-181, 06:319; 06:102 (Kanban "exact count") |
| H8 | **Estimates are recorded declared inputs with provenance (who/what/when/scale), re-emittable as history; the team owns the number; per-item est-vs-actual is an "analysis pointer" quoted at batch/wave/group grain; no bare point estimate where the source is a range.** | 03:52-58, 03:531-537; 05:200, 05:234; 07:248; 02:92, 02:282; 06:320 |
| H9 | **Manifests and CI: SKILL.md frontmatter keys only `name/description/license/allowed-tools/metadata/compatibility`, `name` = dir, description ≤ 1024, opt-outs under `metadata:`; no `tools:` in any AGENT.md; copied fixtures pinned in `check-skill-dupes.mjs` GROUPS; `npm run gen:marketplaces` after adding a skill; `npm test && npm run validate` green (incl. the gates CI does not run).** | 04:126-135, 04:147, 04:117, 04:368-378, 04:448-451 |
| H10 | **Vocabulary: no octobots/dual-mode/markers/taskbox/relay; adopt the repo's existing hierarchy words (`campaign → wave → unit → case`, `epic → US-NNN → TASK-NNN → Gn`) and map any new term (`mission`, `cadence`) explicitly; keep raw source words beside canonical categories.** | 04:220-226 (CLAUDE.md:131-133); 03:40-42, 03:585-586; 04:444-447; 07:246; C1 (verified grep) |

---

## 4. Consolidated glossary (adopt these; deviations noted)

### 4.1 Work-item hierarchy across the two factories

| Level (canonical name for the spec) | feature-development | test-automation | manual-qa | Nearest industry term (07:247) | Identity today |
|---|---|---|---|---|---|
| **campaign** (top container) | epic (`# [EPIC] …`, tracker issue) — 02:24; plan file `docs/superpowers/plans/…` | **campaign** (`plan.campaign` slug; card `.agents/automation/campaigns/<slug>.md`) — 03:24 | — | initiative / project | epic issue # or plan file; campaign slug |
| **story** (value slice; optional) | `US-NNN` — 02:25 | — | — | story / feature | `US-NNN` |
| **wave** (gate/merge batch) | dependency **group** `Gn` (plan-text only, 02:28, 02:49) | **wave** (`plan.waves[].slug`, branch `tests/batch-<wave>`, `report.json`) — 03:25; **batch** = a wave with no campaign — 03:26 | **run** `RUN-YYYY-MM-DD-NNN` — 03:37 | sprint / cycle / epic | `Gn` (not machine-readable); receipt dir path `<campaign>/<wave>` (unique, 03:469) |
| **task** (dispatch-and-review unit) | `TASK-NNN` (branch `task/task-NNN`, commit prefix, merge subject) — 02:27, 02:134 | **unit** = cluster or solo case (branch `tests/<ID>-…`) — 03:28 | — | story / issue | `TASK-NNN`; unit branch |
| **case** (leaf) | acceptance criterion `AC-N` (not a work item) — 02:26 | **case** (TMS id, opaque: `TC-101`, `ELITEA-2312`; `TD-123` for technical units) — 03:29-30 | **TC** — 03:37 | sub-task / test case | case id (repeats across generations — never unique alone, 03:468) |
| "mission" | loose prose only (C1) — do **not** use as a level unless defined = "one campaign's in-flight delivery" (02:46) | none | none | — | none |

`work_item_level` in the tokenomics export is `'feature'` by code default, `"batch"` in the template (C15): the spec's level names must map onto it.

### 4.2 Timestamp events (canonical event vocabulary → where the clock exists today)

| Event | feature-dev source (local-branch mode / PR mode) | test-automation source | Exists as a machine stamp today? |
|---|---|---|---|
| `created` | plan commit (one stamp for all tasks, 02:68,269) / `gh issue create` | verdicts file (no date, 03:462) / campaign card Log (prose, 03:347) | batch-only / no |
| `estimated` | `**Complexity:**` in plan header (no date) | `.agents/estimation/<scope>-scored.json` (`estMin/lowMin/highMin/size/sp/confidence`, no date, 03:106-114) | value yes, time no |
| `dispatched` (= **started**) | reflog `branch: Created` (local, expiring, 02:260); Agent-tool `.meta.json` `description` (C20) | `scope.declaredAt` (batch grain, 03:566); first dispatch naming the id (not stored, 03:453-455) | **no durable per-item start** (01:624-625) |
| `first_commit` | first `TASK-NNN:` commit (02:125) | first commit on `tests/<ID>-…` | yes (git) |
| `review_requested` / `in_review` | PR `createdAt` (PR mode) / nothing (local) | first `review*` dispatch label (07:249) | PR mode only |
| `review_returned` (FAIL round) | `TASK-NNN: address review N` commit (02:126,133) / `CHANGES_REQUESTED` review | `fix round N` dispatch label (01:230) | proxy (dev-side) |
| `done` | merge commit `merge task/task-NNN (Rio: PASS…)` (local, batched per group, 02:127,234) / PR `mergedAt` (squash deletes the branch, C19) | case: `scopes/<sid>.json.outcomes[id].at` (03:428) or receipt outcome (lags, 03:393); wave: `gate-runs.jsonl` `{verdict:'green', at}` (03:565); campaign: card Log (prose) | case/wave yes (when declared/gated); task local yes; PR mode via `gh` |
| `cancelled` / `abandoned` | none (branches persist, 02:130) | receipt outcomes `blocked \| un-automatable \| not-started \| infra-stalled` (03:375-386) | test-automation only |
| `blocked` | tracker comment `🚫 Blocked` (chat/tracker only, 02:113) | receipt `blocked`, `parked[]` (03:369) | no interval stamps |
| dispatch `ended` | `live/<session>.jsonl` `endedAt` (01:172-175) | same | yes (opt-in tokenomics) |
| session `started/ended` | ledger `startedAt/endedAt/wallMin/activeMin` (01:153-155) | same | yes (opt-in tokenomics) |

### 4.3 Metric names and the exact definitions this repo should adopt

| Name (field) | Definition to adopt | Unit / aggregation | Where industry or repo vocabulary disagrees |
|---|---|---|---|
| `lead_time` | first `created` stamp of the item → first `done`; **label the start clock** ("plan-tracked, not idea-to-done") | hours; `{n, median, p85, p90?, max}`; excluded zero-duration count printed | Vault: first-seen→done (05:67); vendors: created→done (07:227); Kanban: request→delivered (06:111); DORA "lead time for changes" = commit→prod — **out of scope**, never call anything DORA (05:164) |
| `cycle_time` | first `dispatched` → first `done`; when `dispatched` is unavailable, `first_commit → done` **must be reported under a different name** (`commit_to_done`) | hours; median + P85 with n; min–max when n < 20 (06:282) | Repo has no per-item start (C3); vault cycle time = executing→done as a minority sample (05:68,138); LinearB/Haystack start at first commit (07:228) |
| `work_item_age` | `dispatched` (or proxy) → now, for items not `done`/`cancelled` | hours per item, vs the P85 line | Kanban Guide (06:103); no repo precedent |
| `throughput` | exact **count** of items reaching `done` per ISO week, per level, zero-filled, partial week flagged | items/week series | Kanban "exact count" (06:102); vault `throughput_per_iso_week` (05:70); never points (H7) |
| `velocity` | median (and mean) of `throughput` over **whole** weeks only; `MIN_WHOLE_WEEKS = 3` else `null` | items/week | Vault definition (05:71); industry uses "velocity" for story points — **do not** (06:176); keep vault meaning or rename `weekly_throughput_median` |
| `cadence` | **define as** `velocity` + the inter-wave/group turnaround (`last done of Gn → first dispatched of Gn+1`, 02:236-252) — or drop the word | items/week; minutes | Vault = ritual cadence (05:207); DORA = deployment frequency (07:234); brief = delivery metric |
| `wip` | items `dispatched` and not `done`/`cancelled` at time t | count series | Kanban (06:101); feature-dev bounds it to 1 per dev by policy (02:116) |
| `review_rounds` | count of `review_returned` events before `done` (local: `address review N` commits; PR: `CHANGES_REQUESTED` reviews) | count; distribution, first-pass share | "rework" is overloaded (C17): tokenomics `rework` = fix-round **spend** (01:230); Pluralsight = 30-day churn (07:104) |
| `first_pass_merge_rate` | share of `done` items with `review_rounds = 0` | % with n | Augment/LinearB (06:304); dataset: 22/33 (02:133) |
| `time_to_merge` (PR mode only) | PR `createdAt → mergedAt` | hours; median | Must carry the vault LABEL "PR open to merge. NOT story lead time. NOT cycle time." (05:90) |
| `queue_wait` | `first_commit` (or PR ready) → `done` where merges are batched per group | hours | Dataset-specific (02:234); no vendor equivalent |
| `active_min` / `wall_min` | **tokenomics' numbers, quoted, never recomputed**: active = Σ gaps ≤ 30 min; wall = last − first stamp of the session | minutes | 01:556-562; manual-qa uses ms wall-clock per TC (03:510); vault uses hours |
| `estimate` (original) | the declared input as recorded: `{scale: tshirt\|sp\|active_min\|range, value, low?, high?, tier?, src, at}`; re-emittable | as declared | Jira/ADO original/remaining/completed (07:232); repo: `Complexity` S/M/L (no scale), scoping `estMin` + band (03:113), `effort_days` = ΣSP/8 derived (03:146) — never call a derived figure an estimate without `src` |
| `actual` | **two columns, never one**: `cycle_time` (this tracker) and `active_min`/`cost_usd` (tokenomics) | hours / minutes / USD | C6 |
| `work_ratio` | `actual ÷ estimate` per item, same unit on both sides (minutes vs `estMin`; hours vs a declared hour estimate) | dimensionless; report **MdMRE, PRED(25), MAE** and the ratio distribution per size class, never MMRE alone | Jira `workRatio` (07:232); Shepperd & MacDonell (06:192-196); tokenomics `estVsActualMin.ratio` at batch grain (01:508) — currently broken in production (C8) |
| `hit_rate` | share of items whose actual falls inside the declared `[low, high]` band, per tier | % with n; only where a range exists (test-automation) | Jørgensen (06:206); no feature-dev source (G21) |
| `same_commit_excluded` | items with `lead_time ≤ threshold` excluded from duration stats, kept in throughput | count, % | Vault 72 s (05:69); Vacanti min 1 day (06:117) — pick threshold, print count |
| `flow_efficiency` | `active_min ÷ cycle_time` — **not in v1** unless active signal is defined | % | 06:95 |
| `delivered` (denominator) | test-automation: `DELIVERED_OUTCOMES = ['delivered','defect-found','automated','merged-sanctioned-red']` (03:390); feature-dev: `done` = merged | count | Two denominators always: per delivered and per examined (03:550) |

**Reporting envelope every number needs** (05:228,248; 06:316): `source`, `n`, `denominator`, `baseline` (may be empty), `generated_at`, `cutoff`, `git_ref/sha`, `is_working_tree`, `method`/`caveat` derived from the data — grouped under **Flow Time / Throughput / Quality** for the assessor (05:37,249).
