# delivery-metrics M1 plan v3 — adversarial review (codex gpt-6-astra, round 3)

Date: 2026-09-17.

## Verdict

needs-changes

15 blockers / 5 majors / 0 minors

Several pasted task gates cannot pass. Beyond those fixture failures, acceptance updates, conflict resolution, imported clock bases, transition validation and parent reduction can produce missing or dishonest figures. The fixes below stay within append-only JSONL, ordinary shared files, advisory locking and best-effort sync; none requires transactions or cross-writer coordination.

Review target: the 3,605-line v3, SHA-256 `4cd98270deecd405e6297177d0cba717886c268c9399f90f4438b82b32301e27`, at repository HEAD `1ee9c14c74c27de7e2c2c4508d41e30fd43981a0`. “Plan” references are lines in `docs/superpowers/plans/2026-09-17-delivery-metrics-m1-slice1.md`; “Spec” references are to v5.1 of `docs/superpowers/specs/2026-09-16-delivery-metrics-design.md`. Executable checks used Node v24.14.1, modules extracted into memory and disposable Git fixtures. No implementation files were written and no complete future implementation suite is claimed to pass.

## Findings

| id | severity | slug | task/step | finding | evidence | concrete change |
|---|---|---|---|---|---|---|
| F1 | blocker | markdown-group-parser | Task 4 steps 3–4; Task 10 step 1 | **The real golden input loses five memberships.** Replacing parentheses with a space creates multiple spaces immediately before a middle-dot delimiter. The alternative whitespace splitter consumes those spaces first, leaving a column beginning with the dot, so its task number is lost. The small fixture does not contain this shape. The extractor exits 4 before producing the dataset. | Plan 1126–1127, 1164, 2834–2848. Running the exact parser on pinned `b1f70d2` returned **30 groups / 54 memberships / 59 headings**, missing TASK-029, TASK-031, TASK-045, TASK-046 and TASK-051. Example: `012 run init/ledger (+ empty assessment inputs) · 029 transitions (+ ticketed)`. Spec §12, AC-3. | Split middle-dot columns independently of their surrounding whitespace, then parse aligned columns; preserve the corrected dependency and continuation handling. Add these real lines to the fixture and require 30/59/59/59 before extraction. |
| F2 | blocker | estimate-revision-fixture-mismatch | Task 3 steps 1, 3–4 | **The pasted registration-observation test expects a revision it never supplies.** `registrationObservations` defaults `estimate_revision` to 0; the test's changed item comes straight from `toCatalogue` without setting it, but expects `rev-1`. Task 5, not this function, owns revision assignment. | Plan 817–834, 979–980, 988. Executing the five core modules' supplied tests produced 28 passes and one failure: actual `sec/run-1/task-task-001/estimated/rev-0`, expected `.../rev-1`. | Set the changed item's `estimate_revision` in this unit fixture, explicitly testing the caller contract, and retain a separate CLI test proving Task 5 assigns 1 from existing history. |
| F3 | blocker | roster-refresh-fixture-missing-agents | Task 5 step 1 — CLI roster fixture | **The revised roster contract makes the existing CLI test impossible.** `initRepo()` installs no roles. The test asks `plan roster` to assert `js-dev,tech-lead`, ignores its exit 2, then expects those roles to have been saved. The discovered participating union is empty. | Plan 1297, 1398–1410 versus 307–324 and 1680. Task 1 now deliberately rejects an asserted roster unequal to the installed union. | Create the two installed agent fixtures before the refresh, assert command success, and retain a negative assertion that a foreign or absent role is rejected. Do not weaken the new roster boundary to satisfy the old test. |
| F4 | blocker | backfill-fixture-history-mismatch | Task 9 step 1 | **The negative backfill fixture contradicts both Git ancestry and its own clock helper.** TASK-001's first commit has already been merged into main, so `isAncestor(first, main)` is true and the expected exception is not thrown. The subsequent epoch assertion is also wrong: `mkGit` advances the clock on `add`, making the first task commit 10:30, not the comment's 08:30; it is inside the 09:30 epoch. | Plan 2647, 2652–2668, 2708–2710, 2741, 2755–2756. Running the supplied backfill assertions in memory failed with `Missing expected exception: a task-branch commit is not on main`. Clock arithmetic: plan add 09:00; task add 10:00; commit 10:30. | Use a genuinely unmerged branch for the ancestry-negative case. Make clock advancement explicit on commits/merges only, or correct every expected clock. For epoch-first selection, assert the earliest qualifying in-epoch same-ref commit instead of expecting no record despite such commits. |
| F5 | blocker | golden-registration-gap-mismatch | Task 10 steps 3–4 | **The golden test expects 31 missing creations that registration has already written.** The builder commits a canonical block containing campaign, missions and tasks. Registration resolves creation for every new catalogue item; therefore campaign and mission creation observations precede the golden cutoff too. Gaps are 0, not 31. Fixing the parser does not make this test pass. | Plan 2870–2874, 2907, 2913; registration loops all `delta.created` at 1612–1614 and 974–977; report counts absent creation at 2497. Spec §6.3. | Expect zero registration gaps for this register-then-backfill fixture. If testing task-only backfill gaps is useful, build a separate deliberately incomplete registration fixture and label it accordingly. |
| F6 | blocker | inner-ignore-doctor-incomplete | Task 12 steps 1, 3, 5 | **Doctor fails its own positive submodule test.** After telemetry becomes a submodule, main-repository `git check-ignore` refuses paths inside it; root checks therefore return 0/3 even when both owned blocks are correctly installed. The test requires 3/3. Doctor also omits the specified disposable-index staging check and Git-lock inspection. | Plan 3270–3275, 3374–3382. Executing the extracted installer against a fresh Git repo returned `bootstrap: created`, then `ignore root: MISSING (0/3)`, `ignore inner: ok (3/3)`, `ok: false`. Spec §6.1, AC-2/16. | Check root-pattern behavior in a disposable main index without the telemetry gitlink, and inner behavior in the telemetry repository; test actual staging in disposable indexes. Inspect lock files diagnostically without removing them. Keep the positive real-submodule fixture. |
| F7 | blocker | acceptance-only-update-dropped | Tasks 3 and 5 — estimate delta | **The normal proposal→human acceptance workflow never becomes accepted in the ledger.** `estimateChange` returns `none` whenever range/unit/tier are unchanged, even when a missing acceptance pair becomes present. The new catalogue says accepted, but no new estimated observation is emitted, so all subsequent reports remain unaccepted. Correcting a stale acceptance without changing the range again has the same problem. | Plan 944–963, 1618–1619. Executing `estimateChange(unacceptedRange, sameRangeWithHumanPair)` returned `none`. Spec §§6.3–6.4, §13 M1 Plan + estimates; templates intentionally start unaccepted at 992 and 3471. | Include acceptance and other estimate-semantic changes in delta detection. Emit the next estimate observation for a newly declared pair on an unchanged proposal; preserve the original accepted estimate and require a fresh pair after a range change. Test proposal→acceptance and stale→fresh acceptance end to end. |
| F8 | blocker | revision-conflict-fallback | Tasks 2 and 6 — current conflict resolution | **Only the first conflicting variant blocks fallback.** When same-id/revision variants disagree on transition or basis, `resolveObservations` exports only the first variant's occurrence key. A lower-ranked observation for the other variant's key survives. Which occurrence is blocked depends on file/arrival ordering. | Plan 684–685, 1892–1905. In-memory ledger with CLI variants for transitions A and B plus git B produced selected occurrence `[["B","git"]]`. Spec D6, §6.2 requires current conflicts to block fallback, with no filename winner. | Carry the union of affected occurrence/basis keys from all current conflicting variants, and quarantine each at that source rank. Test both file orders and a higher unique correction that clears the conflict. |
| F9 | blocker | observation-version-retry-conflict | Tasks 2, 5, 9 and 11 — semantic identity | **Capture-version changes break retries, while an actual eligibility fact is ignored.** `factKey` includes current `meta.version` and raw labels. Backfill after a plan version bump regenerates the same SHA/item/event identity with a new version, producing ID-CONFLICT for unchanged historical facts; a CLI retry after a re-cut does likewise. Conversely, `meta.acceptance` is omitted although replay uses it to exclude stale acceptance, so accepted/stale variants coalesce. | Plan 637–639, 1630, 1935, 2747–2748. Executed checks: otherwise identical v1/v2 records have unequal semantic keys; accepted/stale-acceptance records have equal keys. Spec §6.2 allows differing capture envelopes/raw labels but requires equivalent estimate facts; §5 retains history across versions. | Define semantic facts independently of the version at capture and presentation labels, preserving those as provenance. Include acceptance eligibility and other facts actually consumed by replay. Add unchanged backfill and CLI retries across v2, plus conflicting acceptance-state records. |
| F10 | blocker | invalid-transition-replay | Tasks 3, 5–7 — validation and quarantine | **Invalid transitions are still accepted and counted.** Bulk `event --from` validates every row against one initial history, so a fresh batch containing done then cancelled appends both. The CLI never switches to Task 6 occurrence selection, and corrections bypass validation wholesale. Registration blindly cancels removed delivered items, creating the same invalid chain during an ordinary re-cut. Metrics retain invalid-chain items in throughput, and parents use their terminal state without propagating quarantine. | Plan 965, 1624, 1645–1659, 1938–1944, 1956–1967, 2234–2244, 2266. An executed done→cancelled item had `flags:["invalid-chain"]`, task throughput 1 and a delivered parent. Spec §§6.2–6.3, §6.9: CLI rejects invalid chains; removing delivered scope never cancels its history. | Validate each candidate against resolved history including preceding successful batch rows and effective timestamps. Apply corrections as replacement candidates before validation. Preserve removed delivered history without emitting cancellation. Exclude quarantined items from throughput and prevent them qualifying ancestors; test batch, correction and delivered-removal paths. |
| F11 | blocker | parent-integration-boundary | Task 6 step 3 — parent reduction | **Landing can turn wholly cancelled scope into delivered throughput.** The all-cancelled branch only runs when there is no explicit parent done; with landing, the next branch sets the mission done despite zero delivered children, then delivers the campaign. Also, scope-cancelled children are discarded before selecting descendant starts, so removing an already-started child can erase the earliest historical start before first parent completion. | Plan 1956–1967. Executed one cancelled task plus mission landing: mission `done`, campaign `done`. Spec §5 requires at least one delivered child, all-cancelled parents cancelled, and historical scope retained. | Require at least one delivered eligible child regardless of explicit landing. Keep historical descendant clocks and cancellation accounting distinct from the current required-child eligibility set. Add all-cancelled-with-landing and earliest-start-child-removed-before-landing fixtures. |
| F12 | blocker | proxy-clocks-promoted-observed | Task 6 step 3; Task 7 metrics | **Replay converts proxy observations into measured facts.** The shared schema admits scope/gate/receipt proxy bases, but replay does not check a dispatch's basis before assigning observed start, and unconditionally labels task done as observed. These records then enter measured cycle time, throughput and estimate accuracy. | Plan 565, 1936, 1941, 2252–2253. Executed scope-proxy dispatch at 09:00 plus gate-proxy done at 10:00 produced `start_basis=observed`, `done_basis=observed`, cycle sample 3,600 s. Spec D6/D8, §§6.8–6.10; M1 cannot derive the deferred automation proxies. | Admit only eligible observed task build starts and qualifying completion bases into M1 measured clocks. Count other supported bases as deferred/proxy evidence without upgrading them. Add a mixed observed/proxy fixture proving no measured throughput or accuracy leaks. |
| F13 | blocker | hook-unvalidated-child-clocks | Task 11 step 3 | **The completion test still accepts partial transcripts as complete.** Any timestamped non-user record after a user record suffices, including a progress row or intermediate tool activity; malformed trailing JSON is skipped. The hook also emits an observed start from a user-only incomplete transcript. Filename correlation does not establish the required completion evidence. | Plan 3114–3118, 3151–3152, 3164–3165. Executing the exact parser on user + progress + torn final JSON returned `complete:true`, with end set to the progress timestamp. Spec §6.6 requires completion evidence and unknown/incomplete handling for partial transcripts; v1 F17. | Define supported completion evidence from the pinned host shape and reject/truthfully classify progress-only, intermediate-tool and truncated transcripts. Until verified evidence exists, retain diagnostics without claiming a complete observed interval. Add these adversarial fixtures, not only the existing user-only case. |
| F14 | blocker | backfill-wrong-completion-history | Task 9 step 3 | **The import window changes which merge becomes first delivery, and missing integration refs are accepted.** `since` is applied before populating the per-item `done` set. A run with qualifying merges on Monday and Wednesday, imported with `--since Tuesday`, labels Wednesday episode-1 instead of recognizing Monday as the first completion and withholding later-episode inference. An unresolved declared integration ref emits observed records anyway under `head_unverified`. | Plan 2740–2746, 2759–2766. Spec §6.7: require a pinned head on the declared integration ref; `--since` limits new imports; later completion requires explicit reopen evidence. | Establish first qualifying delivery within the generation before applying the import filter; skip later merges unless the deferred episode capability is available. Reject missing/unverifiable integration refs with exit 2 instead of minting observed facts. Test first merge before `since`, later merge after it, and an unknown integration ref. |
| F15 | blocker | cancelled-share-wrong-cohort | Task 8 step 3 — Markdown Quality | **The emitted cancellation fraction uses the entire catalogue, not the creation cohort, and ignores class filtering.** The numerator is every currently cancelled row and the denominator every row at that level. A task created and cancelled before the requested window therefore changes the reported window's cancellation share. This extra metric also has no corresponding JSON metric definition. | Plan 2546 versus 2232–2238 and 2498. Spec §§6.9–6.10 defines cancelled_share over the creation cohort. `p.items` remains unfiltered even when metrics are class-filtered. | Either defer this nonessential M1 figure explicitly and remove the rendered ratio, or compute a single filtered creation-cohort metric in `computeMetrics` and render the same numerator/denominator in both formats. Add carried-in cancellation and class-filter examples. |
| F16 | major | invalid-stable-id-migration | Tasks 3 and 5 — supersedes | **The plan accepts migration declarations it does not implement.** The CLI checks old/new endpoints but never validates `supersedes.run` against the predecessor, applies the map, or rejects nonidentity mappings. A claimed old→new continuity is silently treated as cancellation plus new work, losing the very history the map purported to preserve. The resolution table's “validation only” narrowing does not change the spec. | Plan 895–898, 1585–1588, 1607–1608, 3571; Spec §5 requires validated predecessor, injective mapping and preserved stable history. | Specify and implement the mapping semantics. If M1 permits renames only with retained stable IDs, reject unsupported nonidentity continuity explicitly and require a matching predecessor; document split/merge lineage separately. Never accept a map and silently ignore it. |
| F17 | major | missing-strata-quality-denominators | Tasks 6–8 — metrics grouping and coverage | **The promised strata and source shares remain incomplete.** Flow groups only by class; throughput only by level; estimates create separate class and tier marginals instead of joint class/tier/basis populations. Known role and mission segmentation are absent. Source information is dropped from Item, and coverage labels observed/plan-commit bases as “sources,” with no dispatched/first-commit/done source shares. One level-wide quality denominator does not describe each class speed stratum. | Plan 1902–1903, 1917–1920, 2246–2276, 2300–2303, 2532–2542. Spec D13/D14, §§6.4, 6.10–6.11 and §13 M1 Coverage; v1 F23. | Preserve selected event source and known role/model metadata, group required compatible populations jointly, and attach matching unknown quality denominators in M1. Emit the three named source-coverage distributions; do not substitute basis counts or invent model/person allocation. |
| F18 | major | report-provenance-not-read-bytes | Task 8 step 3 | **Plan hashes can describe different bytes from those used for metrics, and metric evidence is discarded.** `listRuns/loadRun` reads and parses each plan, but `assemble` reopens it later to hash it. A last-writer-wins update between reads makes the report's hash identify a different plan. Occurrence path:line provenance never reaches Item, metrics or the exported report. This violates byte identification even under a non-atomic snapshot caveat. | Plan 930–933, 1902–1903, 1917–1920, 2476, 2480, 2514. Spec §6.11 explicitly requires hashing buffers actually read and metric evidence locators; v1 F24. | Return parsed plan plus path/hash from one buffer read and carry it into the envelope. Preserve relevant selected observation references through timeline/metric derivation. No manifest, transaction or coherent multi-file snapshot is needed. |
| F19 | major | missing-honesty-caveats-gaps | Tasks 8, 11 and 12 — coverage | **Capture diagnostics and unattributed dispatches disappear from delivery coverage.** The hook writes unknown-role/no-transcript/incomplete diagnostics, but reports never read them; admitted null-item dispatches are filtered out without an unattributed denominator. Selecting one registered plan misclassifies every other registered plan's events as unregistered because `known` contains only selected runs. Required capability labels such as producer compatibility and association-history limits are also absent. | Plan 2480, 2489–2495, 2467, 2516–2517, 3131, 3141–3152, 3383–3385. Spec §§6.6, 6.10–6.11, §8; v1 F25. Doctor exposes only total diagnostic lines, not their meaning. | Build registration knowledge independently of report selection; count selected admitted dispatches before item filtering. Read diagnostic kinds for observable capture gaps, retain unknown loss totals, and include the applicable unconditional limits. Test two registered runs with `--plan` selecting one and unbound/incomplete/unattributed capture. |
| F20 | major | cli-exit-window-contract | Tasks 3, 5 and 8 — input validation | **Malformed user input still becomes INTERNAL, and unchecked profile values can defeat metric floors.** For example `missions:{}` throws inside `validatePlan` rather than returning SCHEMA-INVALID; `event --from` of a missing file yields raw ENOENT→exit 1. Profile validation checks only key names, accepting `minWholeWeeks:1`, which immediately permits a one-week velocity contrary to D11. | Plan 884, 889, 1656, 1693–1695, 1707, 2228, 2268. Spec D11/D21, §§6.3–6.5; Global Constraint 8 at Plan 24. | Validate array/object/scalar shapes before iteration, map expected file/input failures to exit 2, and validate profile types/ranges including the three-week minimum. Add malformed-tree, missing-import-file and below-floor profile tests. |

## Checked and fine

- All supplied paths tests passed in memory, including arbitrary linked-worktree ownership before telemetry exists, reversible percent encoding and advisory stale-lock behavior.
- The shipped factory-role map and installed-role intersection now define a concrete runtime roster; the remaining roster finding is the stale Task 5 fixture, not an absent distribution mechanism.
- All six supplied event-module tests passed: strict UTC-ms clocks, fatal UTF-8 decoding, malformed path:line reporting, EOF records, highest unique revision recovery and ordinary retraction.
- All supplied timeline tests passed, including Monday first completion retained through Tuesday scope addition, pending current scope, a fresh landing requirement and non-backdated scope removal.
- The per-item pinned creation resolver and saved request observations address round-2's default-clock and retry design gaps; the new historical fixture's 146 h and 98 h calculations are correct.
- All five supplied metrics tests passed; independently checked 15-second precision, 50 h lead, 56 h parent elapsed, 1.12 ratio, [-4 h,+16 h] band, 1.5-second MAE and nearest-rank calculations.
- UTC ISO year-boundary examples and separate three-whole-covered-week velocity logic passed; late coverage does not enter the velocity denominator.
- The corrected open-item age is 255 h, and the report normalizes default window timestamps before comparison.
- The positive local Git backfill assertions passed: earliest qualifying merge, second-parent evidence, pinned-head growth isolation and unnumbered rework recognition.
- The pinned source has 11 rework-proxy items under the current review regex; the golden extraction blocker is membership parsing, not that regex.
- The sync code checks unmerged/unfinished-merge state before staging and contains a bounded fetch/merge/push retry; finally-sync is present for partial CLI writes.
- The offline installed-smoke URL rewrites match the installer's actual clone URLs and unconditional fetch behavior at `bin/init.mjs:1140–1175`; no network-dependent external validation is falsely claimed.
- SKILL.md frontmatter follows the repository's scalar/no-tools convention; orphan registration, both factory skill lists, on-demand role wiring and marketplace regeneration are specified.
- No dependency package, transaction manifest, global writer lock, per-person metric or cost recomputation is introduced; PR, automation, HTML, calibration and archive rendering are explicitly deferred.

```json findings
{"version": 3, "verdict": "needs-changes", "counts": {"blocker": 15, "major": 5, "minor": 0}, "findings": [{"id": "F1", "severity": "blocker", "slug": "markdown-group-parser", "title": "Golden input loses five memberships after parenthetical stripping", "sections": ["Task 4 steps 3–4", "Task 10 step 1"]}, {"id": "F2", "severity": "blocker", "slug": "estimate-revision-fixture-mismatch", "title": "Registration-observation test expects an unsupplied estimate revision", "sections": ["Task 3 steps 1, 3–4"]}, {"id": "F3", "severity": "blocker", "slug": "roster-refresh-fixture-missing-agents", "title": "CLI roster fixture asserts roles it never installs", "sections": ["Task 5 step 1"]}, {"id": "F4", "severity": "blocker", "slug": "backfill-fixture-history-mismatch", "title": "Negative backfill fixture contradicts ancestry and its clock helper", "sections": ["Task 9 step 1"]}, {"id": "F5", "severity": "blocker", "slug": "golden-registration-gap-mismatch", "title": "Golden expects creation gaps already filled by registration", "sections": ["Task 10 steps 3–4"]}, {"id": "F6", "severity": "blocker", "slug": "inner-ignore-doctor-incomplete", "title": "Doctor root ignore probes fail inside a telemetry submodule", "sections": ["Task 12 steps 1, 3, 5"]}, {"id": "F7", "severity": "blocker", "slug": "acceptance-only-update-dropped", "title": "Accepting an unchanged estimate emits no ledger observation", "sections": ["Tasks 3 and 5 — estimate delta"]}, {"id": "F8", "severity": "blocker", "slug": "revision-conflict-fallback", "title": "Only one conflicting variant blocks lower-source fallback", "sections": ["Tasks 2 and 6 — current conflict resolution"]}, {"id": "F9", "severity": "blocker", "slug": "observation-version-retry-conflict", "title": "Capture version breaks retries while acceptance eligibility is ignored", "sections": ["Tasks 2, 5, 9 and 11 — semantic identity"]}, {"id": "F10", "severity": "blocker", "slug": "invalid-transition-replay", "title": "Invalid chains are accepted and still qualify throughput and parents", "sections": ["Tasks 3, 5–7 — validation and quarantine"]}, {"id": "F11", "severity": "blocker", "slug": "parent-integration-boundary", "title": "Landing delivers wholly cancelled scope and historical starts are discarded", "sections": ["Task 6 step 3"]}, {"id": "F12", "severity": "blocker", "slug": "proxy-clocks-promoted-observed", "title": "Proxy dispatch and completion are promoted to observed cycle time", "sections": ["Task 6 step 3", "Task 7 metrics"]}, {"id": "F13", "severity": "blocker", "slug": "hook-unvalidated-child-clocks", "title": "Progress and truncated transcripts satisfy hook completion", "sections": ["Task 11 step 3"]}, {"id": "F14", "severity": "blocker", "slug": "backfill-wrong-completion-history", "title": "Import filtering redefines first delivery and missing integration refs pass", "sections": ["Task 9 step 3"]}, {"id": "F15", "severity": "blocker", "slug": "cancelled-share-wrong-cohort", "title": "Cancellation share ignores creation cohort and class filtering", "sections": ["Task 8 step 3"]}, {"id": "F16", "severity": "major", "slug": "invalid-stable-id-migration", "title": "Supersedes declarations are accepted without applying continuity", "sections": ["Tasks 3 and 5 — supersedes"]}, {"id": "F17", "severity": "major", "slug": "missing-strata-quality-denominators", "title": "Required strata, source shares and matching quality denominators are absent", "sections": ["Tasks 6–8 — metrics grouping and coverage"]}, {"id": "F18", "severity": "major", "slug": "report-provenance-not-read-bytes", "title": "Plan hashes reread files and metric evidence is dropped", "sections": ["Task 8 step 3"]}, {"id": "F19", "severity": "major", "slug": "missing-honesty-caveats-gaps", "title": "Reports lose capture diagnostics and miscount unregistered events", "sections": ["Tasks 8, 11 and 12 — coverage"]}, {"id": "F20", "severity": "major", "slug": "cli-exit-window-contract", "title": "Invalid input yields INTERNAL and profile values defeat metric floors", "sections": ["Tasks 3, 5 and 8 — input validation"]}]}
```
