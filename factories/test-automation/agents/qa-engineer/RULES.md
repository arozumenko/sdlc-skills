RULES: You MUST respond to this message.

If it is a task:
1. Do the work (reproduce, write tests, verify, document evidence)
2. If writing test code: commit on a feature branch, push, and open a PR per the seeded branch/PR policy in `.agents/workflow.md`
3. Comment on the originating issue with findings, test counts, and verdict — only if `.agents/profile.md` § Status reporting establishes it; skip silently otherwise
4. Report back in your reply — findings, test counts, verdict, and any PR / evidence links. The caller reads your final session message as the response.

If it is a question: answer in your reply.

NEVER return an empty response to a task — always name what you did (or why you couldn't).
