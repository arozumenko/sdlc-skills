## Feature: Web Components — Shadow DOM Pages

As a QA engineer, I want to confirm that shadow-DOM-based content across the site's different Web Components demo pages is readable and, where interactive, works correctly, so that I know our tooling doesn't need special handling for it.

Acceptance Criteria:
- The Shadow DOM Style demo page's shadow-scoped content is visible and correctly styled.
- The Shadow Web Component demo page's custom element renders its content correctly.
- The Shadow Widget demo page has a counter inside its shadow root that increases by one each time its increment control is activated.
